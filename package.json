{
  "name": "napplet",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo run build",
    "dev:site": "turbo run dev --filter=@napplet/web --filter=@napplet/docs",
    "test": "turbo run test:unit",
    "test:unit": "turbo run test:unit",
    "test:e2e": "turbo run test:e2e",
    "test:conformance": "node packages/conformance-cli/dist/cli.js tests/fixtures/napplets/conformant",
    "benchmark:creation": "node scripts/benchmark-napplet-production.mjs --run-agent codex",
    "lint": "turbo run lint",
    "type-check": "turbo run type-check",
    "check:links": "node scripts/check-links.mjs",
    "version-packages": "changeset version && node scripts/sync-jsr-versions.mjs",
    "publish-packages": "turbo run build && pnpm publish -r --access public --no-git-checks",
    "publish-jsr": "node scripts/sync-jsr-versions.mjs && pnpm -r --filter='./packages/*' exec npx jsr publish --allow-slow-types --allow-dirty"
  },
  "devDependencies": {
    "@changesets/cli": "^2.30.0",
    "happy-dom": "^20.10.3",
    "nostr-tools": "^2.23.3",
    "playwright": "^1.59.1",
    "turbo": "^2.9.14",
    "typescript": "^5.9.3",
    "vite": "^6.4.3",
    "vitest": "^4.1.2"
  },
  "packageManager": "pnpm@10.8.0"
}

---
"@napplet/conformance": minor
---

Validate NIP-5D manifest events instead of private HTML manifest metadata in the default conformance path. Adds `validateManifestEvent` and napplet manifest kind exports while keeping `validateManifest(html)` as an HTML-only compatibility wrapper.

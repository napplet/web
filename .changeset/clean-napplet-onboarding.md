---
"@napplet/cli": minor
---

Add the first-party `napplet` CLI for CLI-first onboarding: project initialization, `.napplet/napplet.json` metadata ownership, agent skill installation, build/deploy commands, and standalone binary packaging.

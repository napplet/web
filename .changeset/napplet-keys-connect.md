---
"@napplet/cli": minor
---

Add `napplet keys connect` NIP-46 remote signer login (nostrconnect QR + bunker:// paste). It pairs a remote signer, stores the resulting nbunksec in the platform keychain, and points `.napplet` signing.keyReference at it.

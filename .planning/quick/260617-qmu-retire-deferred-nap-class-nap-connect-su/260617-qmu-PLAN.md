---
phase: quick
plan: 260617-qmu
type: execute
wave: 1
depends_on: []
files_modified:
  - specs/SHELL-CLASS-POLICY.md
  - specs/SHELL-CONNECT-POLICY.md
  - README.md
  - skills/build-napplet/SKILL.md
  - packages/core/src/types/shell.ts
  - packages/core/src/types/global.ts
  - packages/core/src/index.test.ts
  - packages/core/README.md
  - packages/shim/src/index.ts
  - packages/shim/src/shell.test.ts
  - packages/shim/README.md
  - packages/nap/src/shell/sdk.ts
  - packages/nap/src/shell/index.ts
  - packages/nap/src/shell/shim.ts
  - packages/nap/src/shell/shim.test.ts
  - packages/conformance/src/shell/reference-shell.ts
  - packages/conformance/src/shell/reference-shell.test.ts
  - packages/conformance/src/validators/envelope.test.ts
autonomous: true
requirements: [AGENTS-RULE-8]
must_haves:
  truths:
    - "specs/ no longer contains SHELL-CLASS-POLICY.md or SHELL-CONNECT-POLICY.md"
    - "No doc/spec file describes NAP-CLASS / NAP-CONNECT as live protocol surface"
    - "NAP-SHELL ShellEnvironment / ShellInitMessage / NappletShell carry NO class field"
    - "shell.init wire shape is { capabilities, services } only — no class"
    - "@napplet/nap/shell exports no shellClass()"
    - "conformance reference shell emits no class in shell.init"
    - "pnpm build, pnpm type-check, pnpm -r test:unit all green"
    - "changesets added for every package whose shipped output changed"
  artifacts:
    - path: packages/core/src/types/shell.ts
      provides: "NAP-SHELL types with no residual class field"
    - path: packages/conformance/src/shell/reference-shell.ts
      provides: "Reference shell that emits canonical class-free shell.init"
  key_links:
    - from: "ShellEnvironment / ShellInitMessage / NappletShell"
      to: "canonical NAP-SHELL (no mention of classes — user-confirmed)"
      via: "remove class field from all three interfaces + all consumers"
      pattern: "class: number | null"
---

<objective>
Retire the deferred NAP-CLASS / NAP-CLASS-* / NAP-CONNECT surface that a prior sweep
left behind. Two layers:

A. DOCS/SPECS — README.md, skills/build-napplet/SKILL.md, and two specs/*.md files
   describe NAP-CLASS / NAP-CONNECT as live protocol.

B. CODE — a residual opaque `class` integer survived the original defer by being
   relocated into NAP-SHELL's `shell.init` environment (`ShellEnvironment.class`,
   `ShellInitMessage.class`, `NappletShell.class`, `window.napplet.shell.class`,
   `shellClass()`). The user has confirmed the **canonical NAP-SHELL spec no longer
   contains any mention of classes**, so this field is orphaned deferred-NAP surface
   under AGENTS.md rule 8 and must be removed from every package, test, and doc. The
   resulting `shell.init` wire shape is `{ capabilities, services }`.

This is AGENTS.md **rule 8 (retire-on-defer)**: a deferred NAP's surface — or a model
justified by it — must not outlive the NAP anywhere, including a field that got
re-homed under a different NAP during the defer.

DO NOT EDIT: any `CHANGELOG.md` (historical record). `AGENTS.md` / `CLAUDE.md`
(rule 8's cautionary example names NAP-CONNECT/NAP-CLASS intentionally). Any `dist/`
file (regenerated by `pnpm build`). The `vite-plugin/src/types.ts:62` "CVE-2025-69873
class / ReDoS" comment (`class` = category, unrelated).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md

<fidelity_findings>
Ground truth verified during planning (executor must re-read exact lines before editing
— the prior sweep left garbled tokens; match on real current content):

DOCS LAYER
1. specs/ inventory: SHELL-CLASS-POLICY.md, SHELL-CONNECT-POLICY.md (both DELETE),
   SHELL-RESOURCE-POLICY.md (KEEP — live NAP-RESOURCE). No specs/ index exists; only
   README.md cross-links the deleted specs (handled in Task 2).
2. README.md @napplet/nap row lists "16 NAP domain subpaths (… resource, connect, class,
   cvm …)" — DOUBLY wrong: connect/class don't exist AND it omits ifc/shell which do.
   Derive the real subpath list from packages/nap/package.json exports:
   `node -e "const p=require('./packages/nap/package.json'); console.log(Object.keys(p.exports).filter(k=>/^\.\/[a-z]+$/.test(k)).map(k=>k.slice(2)).join(', '))"`
   Drop connect/class from the "ownership-aware …" clause too.
3. README.md @napplet/vite-plugin row describes a `connect?: string[]` option and a
   `strictCsp` deprecation / "shell is now the sole runtime CSP authority". Neither
   exists in packages/vite-plugin/src/types.ts (real options: nappletType, requires,
   artifactMode, configSchema, archetype roles). Replace with accurate description.
4. README.md changelog bullet "v0.29.0 — NAP-CONNECT + Shell as CSP Authority" — remove
   the whole bullet.
5. README.md ASCII lifecycle diagram has two garbled rows ("Class assignment
   (class.assigned) … window.napplet.connect" / "Connect grant injection … 
   window.napplet.class") — remove both; keep box-drawing coherent.
6. skills/build-napplet/SKILL.md: frontmatter description clause about NAP-CLASS +
   NAP-CONNECT (remove, keep NAP-IDENTITY clause); the entire "## Step 11 — Two-class
   posture …" section (remove, then renumber Step 12→11, Step 13→12); two gotcha bullets
   ("Do not assume `window.napplet.class` …", "Cleartext `http:`/`ws:` origins silently
   fail …"). LEAVE Step 10 NAP-RESOURCE incl. its `connect-src 'none'` sandbox-CSP prose,
   and the live "don't call fetch" `connect-src 'none'` gotcha — those describe the
   iframe sandbox, not NAP-CONNECT.

CODE LAYER — the residual NAP-SHELL `class` field. Remove from SOURCE (not dist/):
7. packages/core/src/types/shell.ts — `class` appears in THREE interfaces:
   - ShellEnvironment: doc line ~55 ("and the napplet's assigned class"), @example
     `class: 1,` (~62), field `class: number | null;` + its `/** opaque class … */` doc (~71-72).
   - NappletShell: field `readonly class: number | null;` + doc (~105-106).
   - ShellInitMessage: @example `class: 1,` (~146), field `class: number | null;` + doc (~156-157).
   Also fix the top-of-file handshake prose (~line 14) "carrying the environment —
   capabilities, services, and class." → drop "and class". Result: both interfaces and the
   wire message carry only `{ capabilities, services }` (+ `type` on the message).
8. packages/core/src/types/global.ts:280 — JSDoc "the napplet's opaque `class`," in the
   NappletShell summary — drop that phrase.
9. packages/core/src/index.test.ts — lines ~102,108,112,120,125: remove `class:` keys from
   ShellEnvironment/init literals and any `expect(env.class)…` assertions.
10. packages/core/README.md:128,136 — remove the `class: number | null` lines from the
    illustrated ShellEnvironment / NappletShell shapes + any "opaque integer the runtime
    assigns" comment.
11. packages/shim/src/index.ts — line ~100 `class: number | null;` (local init type),
    line ~104 `shell.class = env.class;`, line ~303 `class: null,` (default env). Remove all
    three so the shim never reads/writes class.
12. packages/shim/src/shell.test.ts — lines ~27,60,71,94,100: remove class assertions and
    class keys from init literals.
13. packages/shim/README.md — lines ~462 ("Named services and the napplet's opaque class:"),
    ~464 (`window.napplet.shell.class; …`), ~477 (drop `shellClass` from the helper list).
14. packages/nap/src/shell/sdk.ts — remove the entire `shellClass()` function + its JSDoc
    (~lines 47-58).
15. packages/nap/src/shell/index.ts — remove `shellClass,` from the barrel export (~line 38).
16. packages/nap/src/shell/shim.ts — remove the `class:` field it writes into the env
    (~line 64, `class: typeof rawClass === 'number' ? rawClass : null,`) and the `rawClass`
    extraction feeding it (read surrounding lines), and the `class: 1,` @example (~line 54).
17. packages/nap/src/shell/shim.test.ts — lines ~10,15,22,29,34,41,46,54: remove class keys
    and `expect(env.class)…` assertions (incl. the `class: 'high'` invalid-type case, which
    only exists to prove the field coerces — delete that whole case if it tests nothing else).
18. packages/conformance/src/shell/reference-shell.ts — line ~61 (option doc), ~176
    (`readonly class: number | null;` option), ~208 (`const klass = options.class ?? null;`),
    ~225 & ~240 (`class: klass,` in the emitted shell.init). Remove all so the reference
    shell emits `{ type:'shell.init', capabilities, services }`.
19. packages/conformance/src/shell/reference-shell.test.ts — lines ~11,16,24,30,34,38:
    remove `class` option args, the inline `class: number | null` types, and
    `expect(out[0].class)…` assertions.
20. packages/conformance/src/validators/envelope.test.ts:116 — remove the `class: null,`
    key from the shell.init fixture (verify the validator under test doesn't assert on it).

NOTE: removing the field from ShellEnvironment/ShellInitMessage is a wire-shape change but
it REMOVES deferred surface to match canonical NAP-SHELL — this is conformance restoration,
not invented surface. Do NOT add any replacement field.
</fidelity_findings>

<grep_gate_tokens>
Final verification (case-insensitive), excluding node_modules, dist, .git, every
CHANGELOG.md, AGENTS.md, CLAUDE.md, .planning/, and the vite-plugin CVE "class / ReDoS"
comment:
NAP-CLASS, NAP-CONNECT, class.assigned, nap:class, nap:connect, window.napplet.class,
window.napplet.connect, window.napplet.shell.class, shellClass, napplet-connect-granted,
napplet-in-granted, connect:scheme, `class: number | null`, `.class` (as a napplet field).
</grep_gate_tokens>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the two deferred shell-policy specs</name>
  <files>specs/SHELL-CLASS-POLICY.md, specs/SHELL-CONNECT-POLICY.md</files>
  <action>
  `git rm specs/SHELL-CLASS-POLICY.md specs/SHELL-CONNECT-POLICY.md`. Re-confirm no
  surviving non-CHANGELOG file links them by path (only expected hit: README.md, fixed in
  Task 2). Commit (stage by explicit path; never `git add -A`; .playwright-mcp/ must NOT be
  staged): `docs(specs): retire deferred NAP-CLASS/NAP-CONNECT shell-policy docs` + Co-Authored-By.
  </action>
  <verify><automated>test ! -e specs/SHELL-CLASS-POLICY.md && test ! -e specs/SHELL-CONNECT-POLICY.md && echo OK</automated></verify>
  <done>Both spec files git-rm'd and committed.</done>
</task>

<task type="auto">
  <name>Task 2: Clean root README.md (table, changelog, diagram)</name>
  <files>README.md</files>
  <action>
  Apply findings 2-5. (a) @napplet/nap row: replace subpath list with the real exports from
  packages/nap/package.json (use the node one-liner above); drop connect/class from the
  ownership-aware clause. (b) vite-plugin row: replace the `connect?: string[]` / strictCsp /
  "sole runtime CSP authority" sentence with the real options (NIP-5A manifest gen,
  `requires` injection, `artifactMode`, optional `configSchema`, archetype role tags). (c)
  remove the "v0.29.0 — NAP-CONNECT + Shell as CSP Authority" bullet. (d) remove both garbled
  diagram rows; keep box-drawing coherent (last row stays `└──`).
  Commit by path: `docs(readme): drop deferred NAP-CLASS/NAP-CONNECT from table, changelog, diagram` + Co-Authored-By.
  </action>
  <verify><automated>! grep -niE "NAP-CONNECT|NAP-CLASS|window\.napplet\.(class|connect)|class\.assigned|connect\?: string" README.md && echo OK</automated></verify>
  <done>README root cleaned; @napplet/nap row matches package.json exports; grep gate passes; committed.</done>
</task>

<task type="auto">
  <name>Task 3: Strip NAP-CLASS/NAP-CONNECT from build-napplet SKILL.md</name>
  <files>skills/build-napplet/SKILL.md</files>
  <action>
  Apply finding 6: remove the frontmatter NAP-CLASS/NAP-CONNECT description clause (keep
  NAP-IDENTITY); remove the entire "## Step 11 — Two-class posture …" section incl. its
  "### Default to NAP-RESOURCE; reach for NAP-CONNECT …" subsection, class.assigned/
  window.napplet.class/connect code, connect:scheme checks, the nap:connect/nap:resource
  fallback ladder, and the "See NAP-CONNECT / NAP-CLASS" link; renumber Step 12→11, Step
  13→12 (fix any in-body "Step 1N" cross-refs); remove the two NAP-CLASS/NAP-CONNECT gotcha
  bullets. LEAVE Step 10 (NAP-RESOURCE) and the live sandbox `connect-src 'none'` prose.
  Commit by path: `docs(skill): remove deferred NAP-CLASS/NAP-CONNECT step and references` + Co-Authored-By.
  </action>
  <verify><automated>! grep -niE "NAP-CLASS|NAP-CONNECT|window\.napplet\.(class|connect)|class\.assigned|nap:class|nap:connect|connect:scheme" skills/build-napplet/SKILL.md && grep -qE "^## Step 11 — Track shell-user identity" skills/build-napplet/SKILL.md && grep -qE "^## Step 12 — Verify conformance" skills/build-napplet/SKILL.md && echo OK</automated></verify>
  <done>description clause + Step 11 section + two gotcha bullets removed; steps renumbered contiguous; Step 10 intact; grep passes; committed.</done>
</task>

<task type="auto">
  <name>Task 4: Remove residual class field from @napplet/core</name>
  <files>packages/core/src/types/shell.ts, packages/core/src/types/global.ts, packages/core/src/index.test.ts, packages/core/README.md</files>
  <action>
  Apply findings 7-10. In shell.ts remove `class` from ALL THREE interfaces (ShellEnvironment,
  NappletShell, ShellInitMessage) — field, JSDoc, and the `class: 1,` @example lines — and fix
  the top-of-file handshake prose to "capabilities and services" (drop "and class"). In
  global.ts drop the "opaque `class`," phrase. In index.test.ts remove class keys + assertions.
  In core/README.md remove the class lines from the illustrated shapes. The shell.init wire
  shape and ShellEnvironment must end as `{ capabilities, services }` (message also `type`).
  Do NOT run a full build here (cross-package); just keep the file self-consistent.
  Commit by path: `refactor(core)!: remove residual NAP-CLASS class field from NAP-SHELL env` + Co-Authored-By.
  </action>
  <verify><automated>! grep -nE "class" packages/core/src/types/shell.ts && echo OK</automated></verify>
  <done>No `class` token remains in core shell.ts; global.ts/index.test.ts/README cleaned; committed.</done>
</task>

<task type="auto">
  <name>Task 5: Remove class field from @napplet/shim</name>
  <files>packages/shim/src/index.ts, packages/shim/src/shell.test.ts, packages/shim/README.md</files>
  <action>
  Apply findings 11-13. In index.ts remove the local `class: number | null;` init type, the
  `shell.class = env.class;` assignment, and the `class: null,` default-env key. In
  shell.test.ts remove class assertions + init keys. In README.md remove the opaque-class
  comment + `window.napplet.shell.class` line and drop `shellClass` from the helper list.
  Commit by path: `refactor(shim)!: drop residual NAP-CLASS class field` + Co-Authored-By.
  </action>
  <verify><automated>! grep -nE "\.class\b|class:" packages/shim/src/index.ts && echo OK</automated></verify>
  <done>shim no longer reads/writes/exposes class; tests + README cleaned; committed.</done>
</task>

<task type="auto">
  <name>Task 6: Remove shellClass from @napplet/nap/shell</name>
  <files>packages/nap/src/shell/sdk.ts, packages/nap/src/shell/index.ts, packages/nap/src/shell/shim.ts, packages/nap/src/shell/shim.test.ts</files>
  <action>
  Apply findings 14-17. Remove the entire `shellClass()` function + JSDoc from sdk.ts; remove
  `shellClass,` from the index.ts barrel; remove the `class:` field + its `rawClass` source +
  `class: 1,` @example from shim.ts; remove class keys/assertions from shim.test.ts (delete the
  `class: 'high'` coercion case entirely).
  Commit by path: `refactor(nap)!: remove shellClass / residual NAP-CLASS field from nap/shell` + Co-Authored-By.
  </action>
  <verify><automated>! grep -rnE "shellClass|class:" packages/nap/src/shell/ && echo OK</automated></verify>
  <done>nap/shell exports no shellClass and writes no class; committed.</done>
</task>

<task type="auto">
  <name>Task 7: Remove class from @napplet/conformance reference shell + tests</name>
  <files>packages/conformance/src/shell/reference-shell.ts, packages/conformance/src/shell/reference-shell.test.ts, packages/conformance/src/validators/envelope.test.ts</files>
  <action>
  Apply findings 18-20. In reference-shell.ts remove the `class` option (doc + `readonly
  class: number | null;`), the `const klass = options.class ?? null;`, and both `class: klass,`
  keys in the emitted shell.init so it emits `{ type, capabilities, services }`. In the two
  test files remove class option args, inline class types, the `class: null,` fixture key, and
  `expect(out[0].class)…` assertions. Confirm the validator under test does not assert on a
  class field; if it does, that validator is itself residual surface — flag in SUMMARY.
  Commit by path: `refactor(conformance)!: reference shell emits class-free shell.init` + Co-Authored-By.
  </action>
  <verify><automated>! grep -rnE "\bclass\b" packages/conformance/src/shell/reference-shell.ts && echo OK</automated></verify>
  <done>reference shell + conformance tests carry no class; committed.</done>
</task>

<task type="auto">
  <name>Task 8: Changesets + green build/type-check/test</name>
  <files>.changeset/</files>
  <action>
  Add a changeset for every package whose shipped output changed: @napplet/core,
  @napplet/shim, @napplet/nap, @napplet/conformance (and @napplet/conformance-cli only if its
  built output changed — it likely does NOT; do not bump test-only changes). On 0.x a breaking
  wire-shape change is a `minor` bump. Write one changeset file under .changeset/ describing the
  removal of the residual NAP-CLASS `class` field from NAP-SHELL `shell.init` (BREAKING CHANGE:
  shell.init no longer carries `class`; `window.napplet.shell.class` and `shellClass()` removed).
  Do NOT run `pnpm version-packages` (publishing is CI-driven).

  Then run the full gates from repo root and make them green:
    pnpm build
    pnpm type-check
    pnpm -r test:unit
  Fix any fallout (e.g. a missed `class` reference a consumer still imports). Re-run until all
  three pass. dist/ changes from `pnpm build` are expected — do NOT commit dist/ (it is
  git-ignored / build output; verify with `git status` that only source/changeset files stage).
  Commit by path: the changeset file(s) only — `chore(changeset): retire NAP-CLASS class field` + Co-Authored-By.
  (If git status shows dist/ as tracked, STOP and flag — do not commit build artifacts.)
  </action>
  <verify><automated>pnpm build >/dev/null 2>&1 && pnpm type-check >/dev/null 2>&1 && pnpm -r test:unit >/dev/null 2>&1 && echo GREEN</automated></verify>
  <done>changesets added for all affected packages; build, type-check, and unit tests all green; no dist/ committed.</done>
</task>

<task type="auto">
  <name>Task 9: Repo-wide verification grep + SUMMARY</name>
  <files></files>
  <action>
  Confirm zero remaining live references to the deferred surface across SOURCE:
  `grep -rniE "NAP-CLASS|NAP-CONNECT|class\.assigned|nap:class|nap:connect|window\.napplet\.(class|connect)|window\.napplet\.shell\.class|shellClass|napplet-connect-granted|napplet-in-granted|connect:scheme" . | grep -v node_modules | grep -v '/dist/' | grep -v '/.git/' | grep -v CHANGELOG | grep -vi 'AGENTS.md' | grep -v '/CLAUDE.md' | grep -v '.planning/'`
  Expected: NO hits. Also verify the NAP-SHELL `class` field is gone from source types:
  `grep -rnE "class: number \| null|readonly class: number" packages/*/src | grep -v dist` → no hits.
  Confirm specs deleted: `test ! -e specs/SHELL-CLASS-POLICY.md && test ! -e specs/SHELL-CONNECT-POLICY.md`.
  The only acceptable surviving `class` token in source is vite-plugin/src/types.ts "CVE …
  class / ReDoS" (category, unrelated). Write SUMMARY.md recording: specs deleted, docs cleaned,
  the NAP-SHELL class field fully removed across core/shim/nap/conformance + tests, changesets
  added, and build/type-check/test green with output captured.
  </action>
  <verify><automated>test -z "$(grep -rniE 'NAP-CLASS|NAP-CONNECT|class\.assigned|nap:class|nap:connect|window\.napplet\.(class|connect)|shellClass|connect:scheme' . | grep -v node_modules | grep -v '/dist/' | grep -v '/.git/' | grep -v CHANGELOG | grep -vi 'AGENTS.md' | grep -v '/CLAUDE.md' | grep -v '.planning/')" && echo CLEAN</automated></verify>
  <done>grep gate returns zero hits; class field gone from source types; specs deleted; SUMMARY written with verification output.</done>
</task>

</tasks>

<threat_model>
The wire-shape change (removing `class` from shell.init) is a deliberate retire-on-defer to
restore conformance with canonical NAP-SHELL (user-confirmed it has no class). No untrusted
input, no new code path; the only risk is an incomplete removal leaving a consumer importing a
deleted symbol — closed by Task 8's green build/type-check/test and Task 9's grep gate.
</threat_model>

<verification>
- specs/SHELL-CLASS-POLICY.md and specs/SHELL-CONNECT-POLICY.md deleted.
- README.md + skills/build-napplet/SKILL.md contain zero deferred-surface tokens.
- NAP-SHELL ShellEnvironment / ShellInitMessage / NappletShell carry no class field.
- @napplet/nap/shell exports no shellClass; conformance reference shell emits class-free init.
- pnpm build, type-check, and -r test:unit all green; no dist/ committed.
- changesets present for every package whose shipped output changed.
- repo-wide grep returns zero deferred-surface hits (excluding allowed paths + CVE comment).
</verification>

<success_criteria>
The deferred NAP-CLASS / NAP-CLASS-* / NAP-CONNECT surface — including the residual opaque
`class` field re-homed under NAP-SHELL — no longer appears anywhere outside historical
changelogs. AGENTS.md rule 8 satisfied across docs AND code. Atomic commits on branch
chore/retire-nap-class-connect, staged by explicit path, build/type-check/test green.
</success_criteria>

<output>
Create `.planning/quick/260617-qmu-retire-deferred-nap-class-nap-connect-su/260617-qmu-SUMMARY.md` when done.
</output>

---
quick_id: 260623-8j8
status: complete
completed: 2026-06-23
commit: 3bc214c
---

# Quick Task 260623-8j8 Summary

Implemented `resource.bytesMany` from the updated NAP-RESOURCE draft in
napplet/naps PR #13 (`nub-resource`).

## Changes

- Added `resource.bytesMany`, `resource.bytesMany.result`, and
  `resource.bytesMany.error` wire types.
- Exposed `bytesMany(urls, opts?)` through:
  - `window.napplet.resource`
  - `@napplet/nap/resource`
  - `@napplet/sdk`
  - `@napplet/shim`
- Reused the core `ResourceBytesItem` and `ResourceErrorCode` types from
  `@napplet/core` to avoid duplicate protocol type definitions.
- Updated conformance envelope specs, invariant counts, representative samples,
  and the reference shell responder.
- Added resource shim tests for ordered mixed bulk results, top-level errors,
  empty input rejection, abort cancellation, and late-result dropping.
- Updated docs/READMEs and added a changeset for changed shipped packages.

## Verification

- `pnpm --filter @napplet/nap test:unit` — 20 files, 104 tests passed.
- `pnpm --filter @napplet/core type-check` — passed.
- `pnpm --filter @napplet/sdk type-check` — passed.
- `pnpm --filter @napplet/conformance test:unit` — 8 files, 63 tests passed.
- `pnpm build` — 12 tasks passed.
- `pnpm type-check` — 16 tasks passed.
- `pnpm -r test:unit` — 15 workspace projects passed.
- `pnpm lint` — exited 0; no lint tasks configured.
- `git diff --check` — passed.
- `git diff --cached --check` — passed.
- `npx --yes aislop scan -d` — 93/100, 0 errors; existing large-file and
  `js-yaml` warnings remain.

## Notes

- The implementation intentionally does not add streaming, chunks, ranges, or
  progress fields; NAP-RESOURCE defines complete Blob delivery only.
- The shell-side resource policy/fetch implementation is outside this package
  surface and remains a runtime responsibility.

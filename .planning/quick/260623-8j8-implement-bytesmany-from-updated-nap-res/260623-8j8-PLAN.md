---
quick_id: 260623-8j8
status: in_progress
created: 2026-06-23
---

# Quick Task 260623-8j8: implement bytesMany from updated NAP-RESOURCE

## Scope

Implement the `resource.bytesMany(urls, opts?)` surface from the updated
NAP-RESOURCE draft in napplet/naps PR #13 (`nub-resource`).

Canonical reference inspected:
- `/tmp/naps-resource-bulk/naps/NAP-RESOURCE.md`
- `https://github.com/napplet/naps/pull/13`

## Requirements

1. Add wire types for `resource.bytesMany`, `resource.bytesMany.result`, and
   `resource.bytesMany.error`.
2. Expose `bytesMany(urls, opts?)` on the resource shim, core global API, and
   SDK wrappers.
3. Preserve NAP-RESOURCE semantics:
   - `urls` must be non-empty.
   - one top-level terminal envelope per bulk request.
   - result `items` preserve input order and length.
   - per-item failures do not discard successful siblings.
   - abort sends `resource.cancel` for the bulk request id and drops late
     terminal envelopes.
4. Add conformance/reference-shell coverage for the new envelope types.
5. Add focused tests for request shape, ordered result resolution, per-item
   failure preservation, top-level error rejection, and abort behavior.

## Verification

- `pnpm --filter @napplet/nap test:unit`
- `pnpm --filter @napplet/core type-check`
- `pnpm --filter @napplet/sdk type-check`
- `pnpm --filter @napplet/conformance test:unit`
- broader repo gates if the focused checks pass.

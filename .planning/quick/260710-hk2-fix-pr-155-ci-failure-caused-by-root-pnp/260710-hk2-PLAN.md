---
quick_id: 260710-hk2
status: complete
created: 2026-07-10
mode: quick
must_haves:
  - Identify the exact failing PR #155 CI step from GitHub Actions.
  - Keep the tutorial conformance test active in root `pnpm test`.
  - Install the Playwright browser dependency in CI before `pnpm test` runs.
  - Verify the focused local command and push a fix commit.
---

# Quick Task 260710-hk2: PR #155 CI Playwright Install

## Scope

Fix the failing `CI / ci` check on PR #155. The job reaches root `pnpm test`,
which now includes `pnpm test:tutorial`; that command runs `napplet-conformance`
and needs Playwright Chromium available on the runner.

## Plan

1. Read the failed GitHub Actions log and confirm the missing executable.
2. Mirror the existing Playwright install pattern from the conformance/link-check
   workflows into `.github/workflows/ci.yml`.
3. Run the focused tutorial test locally and lightweight workflow checks.
4. Commit, push, and confirm PR #155 reruns CI on the new head.

## Verification Targets

- `pnpm test:tutorial`
- `pnpm test`
- `git diff --check`
- GitHub PR #155 check readback after push

---
status: in-progress
created: 2026-07-06
quick_id: 260706-lc6
slug: resolve-napplet-web-119-conformance-refe
---

# Quick Task 260706-lc6: Resolve napplet/web#119

## Goal

Fix conformance reference-shell support for NAP-RESOURCE so napplets that declare
`resource` can call `window.napplet.resource.bytes(dataUrl)` under conformance,
without granting ambient fetch or breaking domain-absence fallback coverage.

## Tasks

1. Map current conformance injection and resource domain surfaces.
   - Files: conformance packages, NAP resource package, fixtures/tests.
   - Done: exact injection and test locations identified.

2. Implement minimal deterministic `resource.bytes` coverage.
   - Action: expose `resource.bytes(url, opts?)` in the reference shell for
     declared/provided `resource`, support `data:` URLs, and preserve absent-domain
     behavior when the conformance scenario removes `resource`.
   - Done: tests prove boot-time `resource.bytes(dataUrl)` succeeds and domain
     absence still verifies fallback.

3. Verify, commit, push, and open PR.
   - Verify: targeted conformance tests, package type/build/unit tests, root
     relevant checks, and `git diff --check`.
   - Done: branch pushed and PR references issue #119.

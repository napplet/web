---
status: in-progress
created: 2026-07-06
quick_id: 260706-l3j
slug: audit-local-skill-scaffolding-workflow-s
---

# Quick Task 260706-l3j: Skill scaffolding boilerplate audit

## Goal

Audit the napplet skill scaffolding workflow and make the boilerplate generator/template
the canonical substrate for new napplet projects instead of teaching agents to recreate
package manager wiring, Vite config, scripts, layout, and conformance setup by hand.

## Tasks

1. Identify the relevant skill and docs surfaces.
   - Files: `packages/skills/skills/*.md`, `packages/skills/README.md`,
     `apps/docs/packages/skills.md`, `apps/docs/guide/getting-started.md`,
     `packages/boilerplate/src/index.ts`, external `github.com/napplet/boilerplate`.
   - Done: current duplication and authoritative template commands are documented.

2. Update skill/docs guidance with a small behavior-preserving diff.
   - Files: targeted skill docs and package docs only.
   - Action: instruct agents to start from `@napplet/boilerplate`, preserve its scripts
     and layout, list project-specific edit points, and run template validation.
   - Done: old manual setup path is removed from the build workflow or reframed as
     existing-app-only guidance.

3. Verify, commit, push, and open PR.
   - Verify: `pnpm --filter @napplet/skills type-check`, `pnpm --filter @napplet/skills test:unit`,
     `pnpm --filter @napplet/boilerplate type-check`,
     `pnpm --filter @napplet/boilerplate test:unit`, `pnpm check:links`,
     `git diff --check`.
   - Done: Lore-style commit on new branch and PR opened.

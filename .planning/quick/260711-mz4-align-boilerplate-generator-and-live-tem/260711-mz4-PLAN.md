---
quick_id: 260711-mz4
status: ready
mode: quick-full
description: Align boilerplate generator and live template with current @napplet/skills guidance, add drift checks, verify, and ship PRs
must_haves:
  truths:
    - Napplet app code relies on runtime-injected window.napplet, calls domains through @napplet/sdk, and uses direct domain presence only for optional fallbacks.
    - Normal Nostr examples are OUTBOX-first; RELAY appears only as an explicitly documented relay-local escape hatch.
    - Manifest requires contains hard requirements only, while optional domains degrade gracefully.
    - CONNECT and CLASS are not advertised as usable package surfaces; any mention is explicitly deferred or retired.
    - Generated projects do not vendor independent skill bodies and point agents to the shipped @napplet/skills package.
    - Starter dependencies match current shipped @napplet package versions and use the vite plugin single-file mode.
    - Regression checks fail when stale bootstrap, probing, skill-fork, requirements, or relay-default guidance returns.
    - Canonical claims remain traceable to live NIP-5D and NAP source heads; @napplet/skills supplies authoring guidance, not protocol authority.
  artifacts:
    - path: packages/boilerplate/README.md
      provides: Generator-facing canonical skill pointer and starter contract.
    - path: packages/boilerplate/test/guidance.test.mjs
      provides: Dependency-free generator guidance regression checks.
    - path: /home/sandwich/Develop/napplet-boilerplate/.codex/skills/README.md
      provides: Small pointer to install and use @napplet/skills without vendored skill bodies.
    - path: /home/sandwich/Develop/napplet-boilerplate/scripts/check-guidance.mjs
      provides: Template-wide stale-guidance regression gate.
    - path: /home/sandwich/Develop/napplet-boilerplate/src/main.ts
      provides: OUTBOX-first starter with optional-domain fallbacks.
  key_links:
    - from: packages/boilerplate/README.md
      to: packages/skills/README.md
      via: official @napplet/skills installation and source-of-guidance pointer
    - from: /home/sandwich/Develop/napplet-boilerplate/package.json
      to: /home/sandwich/Develop/napplet-boilerplate/scripts/check-guidance.mjs
      via: test:guidance and verify scripts
    - from: /home/sandwich/Develop/napplet-boilerplate/src/main.ts
      to: /home/sandwich/Develop/napplet-boilerplate/vite.config.ts
      via: optional domain checks and hard-only requires declaration
---

# Quick Task 260711-mz4 Plan

## Task 1: Guard generator guidance against template drift

**Files:** `packages/boilerplate/README.md`, `packages/boilerplate/package.json`, `packages/boilerplate/test/guidance.test.mjs`, `packages/skills/src/index.test.ts`, `.changeset/*.md`

**Action:** Point generator users to current `@napplet/skills`; state runtime injection, SDK-first calls, optional-domain checks, OUTBOX-first access, hard-only requirements, and relay-local escape-hatch boundaries. Keep protocol claims cited to the live NIP-5D/NAP sources rather than treating skills as normative. Add dependency-free tests that reject active stale guidance and assert generator/skills alignment. The guards must cover app-owned shim imports, `shell.ready`, `shell.supports`, `window.napplet.shell`, `discoverServices`, `hasService` variants, active connect/class surfaces, default `relay.query`/`relay.subscribe`/`relay.publish`, optional domains promoted into `requires`, and vendored local `SKILL.md` bodies. Add a patch changeset for shipped boilerplate docs.

**Verify:** Build and run unit tests for `@napplet/boilerplate` and `@napplet/skills`; scan changed monorepo surfaces and generated output for forbidden active terms, allowing connect/class only in explicitly deferred/retired text and shim only as runtime plumbing; compare documented domains with package exports; run type-check and diff checks.

**Done:** Published generator guidance and tests encode the same authoring contract as current `@napplet/skills` without changing protocol wire surface.

## Task 2: Finish live template PR #4

**Files:** `.codex/skills/**`, `AGENTS.md`, `README.md`, `docs/**`, `src/main.ts`, `index.html`, `config.schema.json`, `vite.config.ts`, `package.json`, `pnpm-lock.yaml`, `scripts/check-guidance.mjs`

**Action:** Delete vendored local skill bodies and add a small official-package pointer. Replace generic relay examples/source with OUTBOX-first behavior, retain RELAY only as a named relay-local escape hatch, distinguish hard requirements from optional guarded enhancements, refresh the living NIP-5D link and package/domain docs, bump shipped package versions, use `artifactMode: 'single-file'`, and add a stale-guidance gate over all tracked docs/source. Preserve explicit deferred CONNECT/CLASS notes and describe shim only as runtime-side plumbing. Add a runtime regression that loads with optional domains absent and proves controls/fallback state remain usable without calls or crashes.

**Verify:** Run `pnpm install`, guidance and absent-domain runtime tests, type-check, build, conformance, `verify`, exact stale-term scans, tracked-skill scan, npm/JSR version queries, installed export-map/API readback, and `git diff --check`. Cite live NIP-5D head plus NAP-OUTBOX/RELAY heads for the claims they own.

**Done:** Template source, docs, dependencies, and automated guard match current NIP-5D plus `@napplet/skills`, with optional features degrading cleanly.

## Task 3: Integrate, review, and ship both repositories

**Files:** Both repository diffs, GSD summary/verification/state artifacts, PR bodies.

**Action:** Review changed files for protocol invention and unrelated WIP, run full monorepo gates plus changed-file AI-slop scan, generate a fresh app through the local template override and verify it, commit with Lore trailers, push both branches, update template PR #4, open the monorepo PR, and verify checks on the exact pushed heads.

**Verify:** `pnpm build`, `pnpm type-check`, `pnpm -r test:unit`, relevant package tests, generated-app build/conformance and optional-domain-absent proof, stale-term audit, 100/100 changed-file slop target, clean branch status, exact local/remote head equality, and passing or explicitly absent `gh pr checks` evidence for each pushed head.

**Done:** Both branches are pushed, PR-ready/open, all required gates pass, original `feat/cli-ux-parity` WIP is unchanged, and remaining upstream NAP/vite-plugin protocol conflicts are reported without being copied.

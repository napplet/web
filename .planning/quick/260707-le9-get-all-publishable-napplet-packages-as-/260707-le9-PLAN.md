---
quick_id: 260707-le9
status: in_progress
created: 2026-07-07
must_haves:
  truths:
    - All JSR-published @napplet packages are identified from current package configs.
    - Current score blockers are measured from live JSR/dry-run evidence, not guessed.
    - Fixes must not invent or change protocol surface.
    - Remaining non-local gaps are reported as operator actions.
  artifacts:
    - Package metadata/docs/source edits that raise JSR score where possible.
    - Per-package JSR dry-run or equivalent evidence.
    - Completion summary with remaining operator guide.
---

# Quick Task 260707-le9: Improve JSR Package Scores

## Goal

Get all publishable `@napplet/*` packages as close as possible to 100 JSR score.
Document any remaining gaps that require operator assistance.

## Tasks

1. Identify package set and score gaps.
   - Files: root package configs, package `jsr.json`/`deno.json`, live JSR package pages or APIs.
   - Action: enumerate publishable packages and record score blockers from current evidence.
   - Verify: package list covers every local JSR publish target and score/gap data is current.
   - Done: clear package matrix exists before edits.

2. Fix local score blockers.
   - Files: package metadata, README/API docs, exported TypeScript sources as needed.
   - Action: improve missing metadata, docs, exports, JSDoc, or source typing without changing protocol.
   - Verify: JSR dry-run and TypeScript gates pass for changed package surfaces.
   - Done: local blockers are removed or reduced to external/operator-only gaps.

3. Verify and ship.
   - Files: `.planning/STATE.md`, quick summary, commits/PR.
   - Action: run repo gates, commit atomic changes, push branch, create PR.
   - Verify: build/type/test/JSR checks pass or operator-only exceptions are documented.
   - Done: PR open with evidence and operator guide for any score gaps that cannot be fixed locally.

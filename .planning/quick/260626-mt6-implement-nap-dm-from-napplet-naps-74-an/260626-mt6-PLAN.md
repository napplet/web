---
quick_id: 260626-mt6
status: completed
created: 2026-06-26
must_haves:
  truths:
    - NAP-DM follows napplet/naps PR #74: domain dm, request/result operations, and dm.message push events.
    - Runtime owns signing, encryption, relay routing, storage, key/session state, and policy.
    - Release PR includes tests and a changeset.
  artifacts:
    - DM package surface under @napplet/nap
    - Thin central shim/sdk forwarding if required by existing package patterns
    - Focused unit tests
    - Changeset
  key_links:
    - https://github.com/napplet/naps/pull/74
---

# Quick Task 260626-mt6: Implement NAP-DM

## Task 1: Map and mirror existing NAP package patterns

Files: `packages/nap/src/**`, `packages/shim/src/**`, `packages/sdk/src/**`, package export maps, existing tests.

Action: Identify the closest request/result and live-event NAP implementations, then derive the DM package shape from those patterns.

Verify: Code references prove the selected pattern already supports request correlation, push events, package subpaths, and type exports.

Done: DM implementation plan is grounded in existing package architecture, not a new abstraction.

## Task 2: Add DM types, SDK, shim, exports, and tests

Files: likely `packages/nap/src/dm/**`, `packages/nap/src/index.ts`, `packages/nap/package.json`, central shim/sdk files if the repo pattern requires them, and focused tests.

Action: Implement `dm.status`, `dm.conversations`, `dm.messages`, `dm.send`, `dm.subscribe`, `dm.unsubscribe`, and `dm.message` with typed payloads and helpers.

Verify: Focused tests cover outbound request payloads, result resolution, error handling, subscription push delivery, and unsubscribe behavior.

Done: Consumers can import the DM NAP surface and build against the PR #74 contract.

## Task 3: Release prep and full verification

Files: `.changeset/**`, planning summary/state, package metadata touched by the implementation.

Action: Add a changeset for shipped package output, run build/type-check/unit tests/static checks, commit atomically, push, and open the implementation PR.

Verify: `pnpm build`, `pnpm type-check`, `pnpm -r test:unit`, relevant focused tests, `git diff --check`, and PR readback all pass.

Done: PR is open with changeset and verification evidence.

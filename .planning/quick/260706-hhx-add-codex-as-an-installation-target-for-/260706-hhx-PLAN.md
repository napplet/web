---
quick_id: 260706-hhx
status: in_progress
description: add codex as an installation target for @napplet/skills skills installer
created: 2026-07-06
---

# Quick Task 260706-hhx: Add Codex Installer Target

## Goal

Add `codex` as a documented installation target for the `@napplet/skills`
installer so users can install shipped napplet skills into Codex's local skill
directory with the same CLI/API shape as existing targets.

## Tasks

1. Update installer target mapping and CLI help.
   - Files: `packages/skills/src/index.ts`, `packages/skills/src/cli.ts`
   - Action: add a `codex` target that writes skill directories under `.codex/skills`
     relative to the selected working directory, matching the existing project-local
     skillDir targets.
   - Verify: focused unit tests cover target exposure and written `SKILL.md` output.
   - Done: `install({ to: 'codex' })` writes each selected skill to
     `.codex/skills/<skill>/SKILL.md`.

2. Sync package docs.
   - Files: `packages/skills/README.md`
   - Action: document `codex` in the CLI target list and API target union.
   - Verify: grep docs for stale target lists.
   - Done: README users can discover and call `napplet-skills install --to codex`.

3. Run focused package gates and repo hygiene checks.
   - Files: test/build outputs only if generated by existing tooling.
   - Action: run `pnpm --filter @napplet/skills test:unit`, package type-check/build
     if needed, `git diff --check`, and the AI-slop gate.
   - Verify: record exact commands and results in SUMMARY.md.
   - Done: evidence proves the new target works and does not introduce obvious quality drift.

---
phase: quick/260524-l1t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ai-slop.yml
  - .github/badges/aislop-score.json
  - README.md
autonomous: true
requirements:
  - QUICK-260524-L1T
---

<objective>
Add a GitHub Actions workflow that runs the AI slop scan and exposes the current score through a README badge.
</objective>

<tasks>

<task type="auto">
  <name>Add AI slop score workflow and README badge</name>
  <files>.github/workflows/ai-slop.yml, .github/badges/aislop-score.json, README.md</files>
  <action>
Create a workflow that runs `pnpm dlx aislop@0.9.3 scan --json .`, converts the report into a Shields endpoint JSON file, and commits the badge source on pushes to `main` when the score changes. Add a README badge that reads that endpoint and links to the workflow run history.
  </action>
  <verify>
Validate the workflow syntax, badge JSON schema, README badge target, and current scan result.
  </verify>
</task>

</tasks>

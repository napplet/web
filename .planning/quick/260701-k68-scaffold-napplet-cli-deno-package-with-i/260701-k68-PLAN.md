# Quick Task 260701-k68: Scaffold @napplet/cli Deno package with init, discovery staging, deploy plan, signing mode parsing, and wrapper command surfaces

**Date:** 2026-07-01
**Status:** in progress

## Scope

Create the first shippable `@napplet/cli` package slice using Deno. This does not claim the full deployer is complete; it establishes the package, command surfaces, config shape, discovery/staging logic, deployment target modeling, signing-method parsing, and wrappers for conformance/Paja.

## Tasks

1. Add `packages/cli` as a Deno workspace package.
   - Files: `packages/cli/package.json`, `packages/cli/deno.json`, `packages/cli/src/*`, `packages/cli/tests/*`
   - Verify: `deno check src/cli.ts`, `deno test tests/`

2. Implement config/init/discovery/deploy-plan/signing-wrapper primitives.
   - Files: `packages/cli/src/config.ts`, `discover.ts`, `deploy-plan.ts`, `signing.ts`, `process.ts`, `cli.ts`
   - Verify: unit tests cover config defaults, traversal discovery, root/named/snapshot expansion, and signing mode classification.

3. Document current scope and remaining deploy/keychain work.
   - Files: `packages/cli/README.md`, `.changeset/*`
   - Verify: package docs do not present upload/publish/keychain as complete.

## Verification

- `pnpm --filter @napplet/cli build`
- `pnpm --filter @napplet/cli test:unit`
- `git diff --check`

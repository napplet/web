---
phase: 157
plan: "157-04"
type: implementation
wave: 4
depends_on:
  - "157-01"
  - "157-03"
files_modified:
  - packages/sdk/src/index.ts
  - packages/sdk/README.md
  - packages/core/src/index.test.ts
  - packages/shim/src/shell.test.ts
requirements:
  - NIP5D-01
  - NIP5D-02
  - PKG-04
autonomous: true
must_haves:
  truths:
    - SDK/domain helpers must fail clearly when a requested domain object is absent.
    - Examples must gate by `if (window.napplet?.relay)` style property presence, not `shell.supports()`.
  artifacts:
    - packages/sdk/src/index.ts
    - packages/sdk/README.md
  key_links:
    - packages/core/src/types/global.ts -> packages/sdk/src/index.ts
---

<objective>
Align SDK guards and package tests with optional injected domains.
</objective>

<tasks>
<task type="auto">
  <name>Update SDK missing-domain guards</name>
  <files>packages/sdk/src/index.ts</files>
  <action>Audit central SDK exports and helper accessors so absent domain objects produce clear errors without consulting shell.supports.</action>
  <verify>`pnpm --filter @napplet/sdk type-check` passes.</verify>
  <done>SDK uses property presence as its availability boundary.</done>
</task>

<task type="auto">
  <name>Run package migration tests</name>
  <files>packages/core/src/index.test.ts, packages/shim/src/shell.test.ts, packages/sdk/README.md</files>
  <action>Update tests and package-level examples for optional domains and missing-domain behavior.</action>
  <verify>`pnpm --filter @napplet/core test:unit && pnpm --filter @napplet/shim test:unit && pnpm --filter @napplet/sdk type-check` pass.</verify>
  <done>Package migration is verified before conformance work starts.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @napplet/core test:unit`
- `pnpm --filter @napplet/shim test:unit`
- `pnpm --filter @napplet/sdk type-check`
</verification>

<success_criteria>
- Package tests and SDK type-check pass with optional injected domains.
</success_criteria>

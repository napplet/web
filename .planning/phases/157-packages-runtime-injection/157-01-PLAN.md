---
phase: 157
plan: "157-01"
type: implementation
wave: 2
depends_on:
  - "156-01"
files_modified:
  - packages/core/src/envelope.ts
  - packages/core/src/types/global.ts
  - packages/core/src/types/shell.ts
  - packages/core/src/index.ts
  - packages/core/src/index.test.ts
requirements:
  - NIP5D-01
  - NIP5D-02
  - NIP5D-03
  - PKG-01
  - PKG-04
autonomous: true
must_haves:
  truths:
    - `NappletGlobal` models runtime-injected optional NAP domain properties; unsupported domains are absent.
    - Active core types do not expose generic `window.napplet.shell`, `ShellEnvironment`, `shell.ready`, or `shell.init`.
    - Any normative comment added by this plan cites NIP-5D PR #2303 or a NAP source instead of local convention.
  artifacts:
    - packages/core/src/types/global.ts
    - packages/core/src/envelope.ts
    - packages/core/src/index.test.ts
  key_links:
    - https://github.com/nostr-protocol/nips/pull/2303
    - packages/core/src/index.ts -> packages/core/src/types/global.ts
---

<objective>
Retire generic shell protocol types from `@napplet/core` and model the current
NIP-5D runtime-injected domain namespace.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Remove core shell protocol surface</name>
  <files>packages/core/src/envelope.ts, packages/core/src/types/shell.ts, packages/core/src/index.ts</files>
  <action>Delete generic shell/capability-query types and exports that are not defined by current NIP-5D PR #2303.</action>
  <verify>`rg "ShellEnvironment|ShellSupports|NappletShell|shell\\.ready|shell\\.init" packages/core/src` returns no live type/export hits outside intentional removal tests.</verify>
  <done>`@napplet/core` no longer exports active generic shell protocol types.</done>
</task>

<task type="auto" tdd="true">
  <name>Make injected domains optional</name>
  <files>packages/core/src/types/global.ts, packages/core/src/index.test.ts</files>
  <action>Change `NappletGlobal` domain fields to optional and update type tests/examples to use property-presence checks.</action>
  <verify>`pnpm --filter @napplet/core test:unit` passes.</verify>
  <done>Tests prove absent domain properties are valid and present domain objects remain typed.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @napplet/core test:unit`
- `pnpm --filter @napplet/core type-check`
- Focused stale-surface grep over `packages/core/src`.
</verification>

<success_criteria>
- No active core type requires `window.napplet.shell`.
- Domain objects are optional and checked by property presence.
</success_criteria>

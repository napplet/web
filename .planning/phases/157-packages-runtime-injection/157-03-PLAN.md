---
phase: 157
plan: "157-03"
type: implementation
wave: 3
depends_on:
  - "157-01"
  - "157-02"
files_modified:
  - packages/shim/src/index.ts
  - packages/shim/src/runtime-guard.ts
  - packages/shim/src/shell.test.ts
  - packages/shim/package.json
  - packages/shim/tsup.config.ts
requirements:
  - NIP5D-01
  - NIP5D-02
  - NIP5D-03
  - PKG-01
  - PKG-03
  - PKG-04
autonomous: true
must_haves:
  truths:
    - Runtime injection installs selected NAP domain objects before napplet code; it does not emit or wait for shell handshake messages.
    - `window.napplet.shell` is not installed by `@napplet/shim`.
    - Runtime injection must stay outside signed artifact bytes; docs/comments must not imply napplets import shim for normative bootstrap.
  artifacts:
    - packages/shim/src/index.ts
    - packages/shim/src/shell.test.ts
  key_links:
    - packages/core/src/types/global.ts -> packages/shim/src/index.ts
    - https://github.com/nostr-protocol/nips/pull/2303
---

<objective>
Refactor `@napplet/shim` from napplet-side shell handshake bootstrap into a
runtime-owned domain injection installer.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Install selectable runtime domain namespace</name>
  <files>packages/shim/src/index.ts, packages/shim/src/shell.test.ts</files>
  <action>Expose an installer that writes selected NAP domain objects to `window.napplet`, registers message routing, and omits unsupported domains.</action>
  <verify>`pnpm --filter @napplet/shim test:unit -- shell` passes and asserts selected-domain install plus absent shell property.</verify>
  <done>Runtime can install all domains or a subset without generic shell surface.</done>
</task>

<task type="auto">
  <name>Remove handshake runtime guard coupling</name>
  <files>packages/shim/src/index.ts, packages/shim/src/runtime-guard.ts, packages/shim/src/shell.test.ts</files>
  <action>Stop emitting `shell.ready`, stop waiting for `shell.init`, and update guard naming/comments/tests to runtime-injection presence rather than handshake readiness.</action>
  <verify>`rg "shell\\.ready|shell\\.init|window\\.napplet\\.shell|ShellEnvironment" packages/shim/src` has zero live-code matches.</verify>
  <done>`@napplet/shim` no longer depends on handshake traffic.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @napplet/shim test:unit`
- `pnpm --filter @napplet/shim type-check`
</verification>

<success_criteria>
- Shim installs domain objects and no generic shell API.
- Tests cover missing-domain absence and selected-domain injection.
</success_criteria>

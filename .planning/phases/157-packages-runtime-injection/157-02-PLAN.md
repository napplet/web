---
phase: 157
plan: "157-02"
type: implementation
wave: 2
depends_on:
  - "156-01"
files_modified:
  - packages/nap/src/shell
  - packages/nap/package.json
  - packages/nap/jsr.json
  - packages/nap/tsup.config.ts
requirements:
  - NIP5D-01
  - NIP5D-03
  - PKG-02
  - PKG-05
autonomous: true
must_haves:
  truths:
    - `@napplet/nap/shell` is removed because current NIP-5D defines no generic NAP-SHELL domain.
    - Package export metadata must not advertise removed protocol surface.
  artifacts:
    - packages/nap/package.json
    - packages/nap/jsr.json
    - packages/nap/tsup.config.ts
  key_links:
    - https://github.com/nostr-protocol/nips/pull/2303
---

<objective>
Remove the stale NAP-SHELL package subpath and export wiring.
</objective>

<tasks>
<task type="auto">
  <name>Delete shell subpath implementation</name>
  <files>packages/nap/src/shell</files>
  <action>Remove shell types, shim, sdk, barrel, and tests from the active `@napplet/nap` source tree.</action>
  <verify>`test ! -e packages/nap/src/shell` succeeds.</verify>
  <done>No source file remains for `@napplet/nap/shell`.</done>
</task>

<task type="auto">
  <name>Remove shell subpath export metadata</name>
  <files>packages/nap/package.json, packages/nap/jsr.json, packages/nap/tsup.config.ts</files>
  <action>Remove shell entries from npm exports, JSR exports, and tsup entry points.</action>
  <verify>`rg "@napplet/nap/shell|src/shell|./shell" packages/nap/package.json packages/nap/jsr.json packages/nap/tsup.config.ts` has zero matches.</verify>
  <done>Consumers cannot import the removed shell subpath from published metadata.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @napplet/nap test:unit`
- `pnpm --filter @napplet/nap type-check`
</verification>

<success_criteria>
- Removed package subpath cannot be resolved through active export maps.
</success_criteria>

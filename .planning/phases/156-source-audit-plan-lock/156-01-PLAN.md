---
phase: 156
plan: "156-01"
type: audit
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/156-source-audit-plan-lock/156-IMPACT-MATRIX.md
requirements:
  - NIP5D-01
autonomous: true
must_haves:
  truths:
    - NIP-5D PR #2303 head 6ca5632 says runtime injects window.napplet before napplet scripts run.
    - Available NAP domains are represented by present domain properties; unsupported domains are absent.
    - Generic window.napplet.shell, shell.supports(), shell.ready, and shell.init are not current normative protocol surface.
  artifacts:
    - .planning/phases/156-source-audit-plan-lock/156-IMPACT-MATRIX.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  key_links:
    - https://github.com/nostr-protocol/nips/pull/2303
    - https://github.com/napplet/naps
---

<objective>
Ground v0.34.0 in live NIP-5D PR #2303 head `6ca5632`, audit stale repo
surfaces, and create executable plans for the runtime-injection migration.
</objective>

<tasks>
<task type="auto">
  <name>Fetch canonical NIP-5D source and record migration truths</name>
  <files>.planning/REQUIREMENTS.md, .planning/STATE.md</files>
  <action>Fetch NIP-5D PR #2303 head 6ca5632, read 5D.md, and record only cited runtime-injection requirements.</action>
  <verify>PR head hash and source path are recorded in planning notes; normative claims trace to NIP-5D or NAPs.</verify>
  <done>Requirements distinguish runtime injection/domain presence from removed generic shell handshake and supports APIs.</done>
</task>

<task type="auto">
  <name>Write repo impact matrix</name>
  <files>.planning/phases/156-source-audit-plan-lock/156-IMPACT-MATRIX.md</files>
  <action>Audit package, conformance, docs, skills, and boilerplate surfaces for stale shell/supports references and classify required follow-up plans.</action>
  <verify>`rg "shell\\.supports|shell\\.ready|shell\\.init|window\\.napplet\\.shell|NAP-SHELL|@napplet/nap/shell"` evidence is summarized by surface.</verify>
  <done>Impact matrix names each migration lane and flags historical/planning exceptions separately from live first-party surfaces.</done>
</task>

<task type="auto">
  <name>Lock executable phase plans</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/157-packages-runtime-injection, .planning/phases/158-conformance-fixtures-injection, .planning/phases/159-docs-skills-boilerplate-injection, .planning/phases/160-release-verification-pr</files>
  <action>Update milestone state and split downstream work into bounded GSD task plans with must_haves, dependencies, verification, and done gates.</action>
  <verify>`gsd-sdk query verify.plan-structure --plan <file>` passes for each new v0.34 plan and `git diff --check` passes.</verify>
  <done>Implementation can proceed from v0.34 runtime-injection plans without stale v0.33 NAP-SHELL assumptions.</done>
</task>
</tasks>

<verification>
- `gh pr view 2303 --repo nostr-protocol/nips` and PR head fetch evidence.
- `rg`/code graph audit evidence for stale surfaces.
- `git diff --check` for planning files.
</verification>

<success_criteria>
- v0.34 requirements map every explicit objective deliverable.
- Phase plans exist for packages, conformance/tooling, docs/skills/boilerplate,
  and release verification.
- No implementation starts from the stale v0.33 NAP-SHELL premise.
</success_criteria>

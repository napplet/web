---
phase: 156
plan: "156-01"
type: audit
wave: 1
depends_on: []
files_modified:
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
requirements:
  - NIP5D-01
autonomous: true
---

<objective>
Ground v0.34.0 in live NIP-5D PR #2303 head `6ca5632`, audit stale repo
surfaces, and create executable plans for the runtime-injection migration.
</objective>

<tasks>
1. Fetch and read `5D.md` from PR #2303 head `6ca5632`; record the canonical
   requirements: runtime injection, domain-object presence, no generic shell
   capability query, no shell handshake.
2. Audit code, tests, docs, skills, boilerplate, and conformance for
   `shell.supports`, `shell.ready`, `shell.init`, `window.napplet.shell`,
   `NAP-SHELL`, and `@napplet/nap/shell`.
3. Update `.planning/PROJECT.md`, `.planning/REQUIREMENTS.md`,
   `.planning/ROADMAP.md`, and `.planning/STATE.md` for v0.34.0.
4. Run plan review against phases 157-160 before broad implementation.
</tasks>

<verification>
- `gh pr view 2303 --repo nostr-protocol/nips` and PR head fetch evidence.
- `rg`/code graph audit evidence for stale surfaces.
- `git diff --check` for planning files.
</verification>

<success_criteria>
- v0.34 requirements map every explicit objective deliverable.
- Phase plans exist for packages, conformance/tooling, docs/skills/boilerplate,
  and release verification.
- No implementation starts from the stale v0.33 NAP-SHELL premise.
</success_criteria>

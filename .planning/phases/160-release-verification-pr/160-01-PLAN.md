---
phase: 160
plan: "160-01"
type: verification
wave: 4
depends_on:
  - "157-01"
  - "157-02"
  - "157-03"
  - "157-04"
  - "158-01"
  - "158-02"
  - "159-01"
  - "159-02"
files_modified:
  - .changeset
  - .planning
requirements:
  - REL-01
  - REL-02
  - REL-03
  - REL-04
autonomous: true
must_haves:
  truths:
    - Full repo gates pass before completion is claimed.
    - Release PR includes changesets for every package with changed shipped output.
    - Stale-surface audit proves no live first-party surface still depends on generic shell protocol.
  artifacts:
    - .changeset
    - .planning
    - GitHub PR readback
  key_links:
    - feat/nip5d-runtime-injection branch
---

<objective>
Prepare the runtime-injection migration for review: changesets, verification,
stale-surface audit, push, and PR.
</objective>

<tasks>
<task type="auto">
  <name>Add release metadata</name>
  <files>.changeset</files>
  <action>Add changesets for every changed shipped package with breaking-change notes tied to runtime injection and removed shell surface.</action>
  <verify>`pnpm changeset status --since=main` reports expected package bumps.</verify>
  <done>Release metadata matches shipped package output.</done>
</task>

<task type="auto">
  <name>Run full verification gates</name>
  <files>.planning</files>
  <action>Run build, type-check, unit tests, conformance, diff check, and AI-slop gate; fix failures before continuing.</action>
  <verify>`pnpm build && pnpm type-check && pnpm -r test:unit && pnpm test:conformance && git diff --check && pnpm dlx aislop scan --json .` pass or any non-applicable gate is explicitly justified.</verify>
  <done>Verification evidence is ready for PR body.</done>
</task>

<task type="auto">
  <name>Prove stale-surface cleanup</name>
  <files>.planning</files>
  <action>Run zero-match/classified-exception grep over live first-party surfaces for stale shell terms.</action>
  <verify>`rg "shell\\.supports|shell\\.ready|shell\\.init|window\\.napplet\\.shell|ShellEnvironment|NAP-SHELL|@napplet/nap/shell" README.md AGENTS.md apps docs packages skills tests` has zero live matches or only documented historical exceptions.</verify>
  <done>REL-03 is proved by concrete grep evidence.</done>
</task>

<task type="auto">
  <name>Commit, push, and open PR</name>
  <files>.planning</files>
  <action>Commit atomic checkpoints with Lore trailers, push branch, create PR, and read back PR status.</action>
  <verify>`gh pr view --json number,state,url,headRefName,statusCheckRollup` confirms open PR on `feat/nip5d-runtime-injection`.</verify>
  <done>PR is open with verification evidence and explicit risks.</done>
</task>
</tasks>

<verification>
- `pnpm build`
- `pnpm type-check`
- `pnpm -r test:unit`
- `pnpm test:conformance`
- `git diff --check`
- `pnpm dlx aislop scan --json .`
- `gh pr view --json number,state,url,headRefName,statusCheckRollup`
</verification>

<success_criteria>
- PR is open and ready for review.
- Full objective stop condition is satisfied or any remaining risk is explicit
  and not a required deliverable.
</success_criteria>

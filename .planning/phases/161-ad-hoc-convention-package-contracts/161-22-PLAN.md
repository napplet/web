---
phase: 161-ad-hoc-convention-package-contracts
plan: "22"
type: execute
wave: 8
depends_on: ["161-16", "161-20", "161-21"]
files_modified:
  - packages/conformance/src/validators/envelope.ts
  - packages/conformance/src/validators/envelope.test.ts
  - packages/conformance/src/shell/reference-shell.ts
  - packages/conformance/src/shell/reference-shell.test.ts
autonomous: true
requirements: [CONV-PKG-05]
must_haves:
  truths:
    - "Conformance recognizes intent.deliver as an inbound carrier with a required delivery object and no ID."
    - "The reference shell accepts a normalized request before separately delivering to a target from an explicit authenticated source."
    - "Caller sender data cannot influence delivered sender."
  artifacts:
    - path: packages/conformance/src/shell/reference-shell.ts
      provides: "Authenticated endpoint fixture, acceptance response, and pending target delivery"
    - path: packages/conformance/src/validators/envelope.ts
      provides: "Adopted intent carrier validation"
  key_links:
    - from: packages/conformance/src/shell/reference-shell.ts
      to: packages/conformance/src/shell/reference-shell.test.ts
      via: "handleFrom endpoint and separate delivery drain"
      pattern: "sender"
---

<objective>
Upgrade conformance from immediate handling fixtures to adopted acceptance and authenticated target delivery.

Purpose: Make reference evidence test the actual carrier/trust boundary without turning local plumbing into protocol.
Output: Envelope registration, explicit endpoint fixture, manifest-derived contracts, pending delivery, and tests.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-PR89-91-DELTA.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-16-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Validate one normalized invoke and one no-ID delivery carrier</name>
  <files>packages/conformance/src/validators/envelope.ts, packages/conformance/src/validators/envelope.test.ts</files>
  <behavior>
    - Normalized intent.invoke with required request object is accepted as a carrier.
    - intent.deliver requires a delivery object and has no correlation requirement.
    - Missing/wrong carriers fail while convention and payload values remain semantically opaque.
  </behavior>
  <action>Register `intent.deliver` as inbound with required object `delivery`; keep request/result IDs only where the adopted table defines them. Extend representative-domain/drift coverage. Validate carrier shape, not convention payload meaning or runtime lifecycle. Do not require INC, a delivery ID, a window ID, or a post-acceptance result.</action>
  <verify><automated>pnpm --filter @napplet/conformance test:unit -- envelope.test.ts</automated></verify>
  <done>Conformance recognizes the exact adopted request/result and delivery carrier separation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Model endpoint-attested acceptance and separate target delivery</name>
  <files>packages/conformance/src/shell/reference-shell.ts, packages/conformance/src/shell/reference-shell.test.ts</files>
  <behavior>
    - handleFrom(authenticated source, invoke) returns an accepted result first and queues target delivery.
    - Taking the target delivery later returns a no-ID IntentDelivery whose sender is the authenticated source, even if the request carries forged sender data.
    - Availability exposes contracts with queryless convention and optional eventKinds; normalized conflicts reject before acceptance.
  </behavior>
  <action>Per D-06/D-09, add an explicit `ReferenceEndpoint` fixture and a source-aware handling entry point. For a normalized supported invoke, return only the accepted result and retain a target delivery keyed to `reference-handler`; expose a separate deterministic method to drain target deliveries after acceptance. Derive sender solely from endpoint.dTag, validate archetype/action/convention consistency before handler resolution, and source availability from queryless contracts including eventKinds. Preserve the generic `attachReferenceShell` web source guard, but do not equate that carrier guard with the carrier-neutral NAP contract. Add no INC event, caller sender, window state, retry, or persistence policy.</action>
  <verify><automated>pnpm --filter @napplet/conformance test:unit -- reference-shell.test.ts &amp;&amp; pnpm --filter @napplet/conformance build &amp;&amp; pnpm --filter @napplet/conformance type-check</automated></verify>
  <done>The reference shell proves authenticated provenance, acceptance ordering, and separate carrier-neutral target delivery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| authenticated endpoint → reference runtime | Endpoint identity, not request data, defines source provenance. |
| reference runtime → resolved target | Only the selected target receives retained delivery data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-53 | Spoofing | reference sender | high | mitigate | Explicit endpoint fixture overrides/ignores caller sender and tests forged input. |
| T-161-54 | Information Disclosure | pending delivery | high | mitigate | Deliveries are keyed/drained only for the resolved reference target. |
</threat_model>

<verification>
Run focused envelope/reference tests, then conformance build/type gates.
</verification>

<success_criteria>
Conformance fixtures demonstrate the adopted carrier and trust model without invented lifecycle or INC semantics.
</success_criteria>

<output>
Create `.planning/phases/161-ad-hoc-convention-package-contracts/161-22-SUMMARY.md` when done.
</output>

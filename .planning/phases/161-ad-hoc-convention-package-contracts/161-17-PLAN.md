---
phase: 161-ad-hoc-convention-package-contracts
plan: "17"
type: execute
wave: 8
depends_on: ["161-15", "161-16"]
files_modified:
  - packages/nap/src/intent/types.ts
  - packages/nap/src/intent/shim.ts
  - packages/nap/src/intent/shim.test.ts
  - packages/nap/src/boundary-smoke.test.ts
autonomous: true
requirements: [CONV-PKG-01]
must_haves:
  truths:
    - "Intent invoke/open derives every wire identity field from the authoritative URI before postMessage."
    - "Every pre-registration delivery is retained and drained once in FIFO order without a public ID."
    - "Accepted results and target delivery are separate, and neither requires INC or a source window lifecycle."
  artifacts:
    - path: packages/nap/src/intent/shim.ts
      provides: "URI invoke/open, result correlation, delivery listener, and internal FIFO"
    - path: packages/nap/src/intent/types.ts
      provides: "Exact no-ID intent.deliver envelope union"
  key_links:
    - from: packages/nap/src/intent/shim.ts
      to: packages/nap/src/convention-uri.ts
      via: "normalize before postToShell"
      pattern: "normalizeConventionUri"
---

<objective>
Implement the adopted intent wire path from authoritative URI to acceptance and independent target delivery.

Purpose: Prove the hardest runtime boundary before facades and docs propagate it.
Output: NAP types, shim behavior, FIFO retention, cleanup, and exhaustive tests.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-PR89-91-DELTA.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-15-SUMMARY.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-16-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace an authoritative URI to an accepted result</name>
  <files>packages/nap/src/intent/types.ts, packages/nap/src/intent/shim.ts, packages/nap/src/intent/shim.test.ts</files>
  <behavior>
    - invoke derives required archetype/action/convention and query payload; open rejects a non-open action before postMessage.
    - All shared invalid URI cases send no envelope, and caller options cannot override derived fields or set sender.
    - `ok: true` resolves normalized fields plus handler; `ok: false` carries a pre-acceptance error; no second result represents delivery.
  </behavior>
  <action>Mirror the D-04 core contract in NAP message types and add `IntentDeliveryMessage { type: 'intent.deliver'; delivery }` only to inbound unions. Change invoke/open to URI/options forms, normalize through Plan 15 before generating the correlated request ID, and keep IDs only on invoke/result pairs. Resolve adopted result branches without synthesizing handling or window state. Add no INC calls, handler authorization shortcut, retry behavior, or lifecycle sequencing to the napplet shim.</action>
  <verify><automated>pnpm --filter @napplet/nap test:unit -- shim.test.ts &amp;&amp; pnpm --filter @napplet/nap type-check</automated></verify>
  <done>The real postMessage path sends normalized wire identity and resolves acceptance independently of delivery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retain and deliver every early no-ID push in FIFO order</name>
  <files>packages/nap/src/intent/shim.ts, packages/nap/src/intent/shim.test.ts, packages/nap/src/boundary-smoke.test.ts</files>
  <behavior>
    - Several deliveries arriving before registration are delivered once, in arrival order, to the first onDelivery handler.
    - A live listener receives later deliveries; closing it stops later callbacks; cleanup clears listeners and retained state.
    - Delivery works with no INC domain/event and carries no request, delivery, window, or handling ID.
  </behavior>
  <action>Implement D-05 as an internal FIFO array plus delivery-listener registry. Route `intent.deliver` before correlated result branches; enqueue every structurally valid early delivery, drain the entire queue in order to the first registering handler, then remove drained items. Keep queue size/capacity and lifecycle policy unobservable. Ensure install cleanup clears pending invoke timers, availability listeners, delivery listeners, and retained deliveries. Test the carrier-neutral path without injecting or sending INC.</action>
  <verify><automated>pnpm --filter @napplet/nap test:unit -- shim.test.ts boundary-smoke.test.ts &amp;&amp; pnpm --filter @napplet/nap build</automated></verify>
  <done>Early and live deliveries are lossless, ordered, no-ID, cleaned up, and carrier-independent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| URI/options → intent.invoke envelope | Caller-controlled input becomes a handler-resolution request. |
| runtime push → onDelivery callback | Runtime-attested provenance accompanies untrusted payload to target code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-47 | Spoofing | delivery provenance | high | mitigate | No sender exists on caller input; tests prove the delivery carrier alone supplies sender. |
| T-161-48 | Denial of Service | early-delivery FIFO | medium | accept | D-05 requires lossless pre-registration retention and intentionally exposes no capacity contract; runtime-level resource policy remains outside the napplet API. |
</threat_model>

<verification>
Run intent and boundary suites with several early deliveries and no INC injection, then build/type-check NAP.
</verification>

<success_criteria>
The NAP implementation exactly separates authoritative invocation, immediate acceptance, and later target delivery.
</success_criteria>

<output>
Create `.planning/phases/161-ad-hoc-convention-package-contracts/161-17-SUMMARY.md` when done.
</output>

---
phase: 161-ad-hoc-convention-package-contracts
plan: "20"
type: execute
wave: 7
depends_on: ["161-03"]
files_modified:
  - packages/vite-plugin/src/types.ts
  - packages/vite-plugin/src/manifest.ts
  - packages/vite-plugin/src/index.test.ts
autonomous: true
requirements: [CONV-PKG-02]
must_haves:
  truths:
    - "Each Vite archetype entry emits one queryless convention contract with zero or more same-tag event kinds."
    - "Archetype metadata remains outside aggregate-hash path folding."
  artifacts:
    - path: packages/vite-plugin/src/types.ts
      provides: "Object metadata with eventKinds?: number[]"
    - path: packages/vite-plugin/src/manifest.ts
      provides: "Per-contract kind tag serialization"
  key_links:
    - from: packages/vite-plugin/src/types.ts
      to: packages/vite-plugin/src/manifest.ts
      via: "Nip5aManifestOptions.archetypes"
      pattern: "eventKinds"
---

<objective>
Emit the adopted manifest contract shape from Vite configuration.

Purpose: Restore same-tag event-kind discovery without restoring numbered protocol negotiation.
Output: Option type, serializer, validation, and exact tag/hash tests.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-PR89-91-DELTA.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-03-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one eventKinds contract into its exact manifest tag</name>
  <reversibility rating="costly">D-07/D-08 change published Vite configuration and signed manifest metadata.</reversibility>
  <files>packages/vite-plugin/src/types.ts, packages/vite-plugin/src/manifest.ts, packages/vite-plugin/src/index.test.ts</files>
  <behavior>
    - No eventKinds emits a three-field tag; one/many kinds append `kind:<unsigned integer>` in configured order.
    - Repeated conventions emit separate tags and kinds never move between contracts.
    - Query-bearing metadata and invalid event-kind numbers reject; aggregate hash stays unchanged for identical path bytes.
  </behavior>
  <action>Per D-07/D-08, evolve the existing object entry to `{ slug, convention, eventKinds?: number[] }`. Require slug to equal the convention archetype, require queryless/fragmentless convention identity, validate event kinds as unsigned integers, and serialize each as a trailing same-tag field. Replace the old no-kind assertion with zero/one/multiple/per-tag isolation tests. Do not infer kinds from payload, combine conventions into one tag, modify path hashing, or add another option spelling.</action>
  <verify><automated>pnpm --filter @napplet/vite-plugin test:unit &amp;&amp; pnpm --filter @napplet/vite-plugin build &amp;&amp; pnpm --filter @napplet/vite-plugin type-check</automated></verify>
  <done>Vite emits exact adopted IntentContract metadata and preserves aggregate identity behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| author config → signed manifest | Untrusted configuration becomes public handler discovery metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-51 | Tampering | contract serialization | high | mitigate | Validate queryless identity and unsigned kinds; exact tests pin per-tag association and aggregate exclusion. |
</threat_model>

<verification>
Run Vite unit/build/type gates including exact signed tag assertions.
</verification>

<success_criteria>
Vite metadata implements D-07/D-08 without adding payload or matching semantics.
</success_criteria>

<output>
Create `.planning/phases/161-ad-hoc-convention-package-contracts/161-20-SUMMARY.md` when done.
</output>

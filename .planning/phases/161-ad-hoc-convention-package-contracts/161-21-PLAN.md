---
phase: 161-ad-hoc-convention-package-contracts
plan: "21"
type: execute
wave: 7
depends_on: ["161-04"]
files_modified:
  - packages/cli/src/types.ts
  - packages/cli/src/config.ts
  - packages/cli/src/manifest-metadata.ts
  - packages/cli/tests/config_test.ts
  - packages/cli/tests/manifest_test.ts
autonomous: true
requirements: [CONV-PKG-03]
must_haves:
  truths:
    - "CLI object config emits optional per-contract event kinds and template kinds survive when config does not replace archetypes."
    - "The convention-only flag and wizard input acquire no kind delimiter or flag."
  artifacts:
    - path: packages/cli/src/types.ts
      provides: "NappletArchetypeConvention.eventKinds"
    - path: packages/cli/src/manifest-metadata.ts
      provides: "Config emission and canonical template preservation"
  key_links:
    - from: packages/cli/src/config.ts
      to: packages/cli/src/manifest-metadata.ts
      via: "normalized metadata.archetypes objects"
      pattern: "eventKinds"
---

<objective>
Preserve and emit adopted per-contract event-kind metadata through the CLI's existing object configuration and templates.

Purpose: Keep CLI deploy round-trips truthful without inventing command syntax absent from the adopted draft.
Output: Types, config validation, metadata merging, and Deno regressions.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-PR89-91-DELTA.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-04-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Round-trip eventKinds through config and template manifest metadata</name>
  <reversibility rating="costly">D-08 changes the published CLI config schema and deployed manifest output.</reversibility>
  <files>packages/cli/src/types.ts, packages/cli/src/config.ts, packages/cli/src/manifest-metadata.ts, packages/cli/tests/config_test.ts, packages/cli/tests/manifest_test.ts</files>
  <behavior>
    - Object config accepts omitted/empty/multiple unsigned eventKinds and emits exact same-tag kind fields.
    - Canonical template tags with kind fields survive when config archetypes are absent; config replacement preserves configured association.
    - Query metadata, invalid numbers, and malformed trailing fields reject; convention-only string parsing remains unchanged.
  </behavior>
  <action>Implement D-07/D-08 on `NappletArchetypeConvention` and object-config normalization. Update canonical archetype-tag recognition to accept only trailing `kind:<unsigned integer>` fields, retain those tags during template merge when archetypes are not replaced, and serialize configured kinds in order. Leave `--archetype`, wizard seed strings, help, and their parser convention-only; do not add a delimiter, prompt, flag, payload inference, or cross-tag aggregation.</action>
  <verify><automated>pnpm --filter @napplet/cli test:unit &amp;&amp; pnpm --filter @napplet/cli type-check &amp;&amp; pnpm --filter @napplet/cli build</automated></verify>
  <done>CLI config/template round-trips exact contract kinds while all command-line convention syntax stays unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| config/template → deployed manifest | Local and imported metadata are merged into a signed public event. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-52 | Tampering | template merge | high | mitigate | Strict same-tag parsing and round-trip tests prevent kind reassignment or silent erasure. |
</threat_model>

<verification>
Run the CLI Deno unit/type/build tasks with object-config and template preservation vectors.
</verification>

<success_criteria>
CLI metadata matches the adopted manifest contract without inventing a command syntax.
</success_criteria>

<output>
Create `.planning/phases/161-ad-hoc-convention-package-contracts/161-21-SUMMARY.md` when done.
</output>

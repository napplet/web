---
phase: 161-ad-hoc-convention-package-contracts
plan: "06"
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - scripts/test-convention-contracts.mjs
  - scripts/test-convention-contracts.test.mjs
autonomous: true
requirements: [CONV-PKG-05, CONV-PKG-06]
must_haves:
  truths:
    - "A permanent executable guard scans the active contract surface before migration work begins."
    - "The guard distinguishes retired convention vocabulary from legitimate WebRTC, URL, Nostr-kind, and workspace dependency uses."
    - "Changelogs, archived planning, and the root skills symlink are not rewritten by the guard."
  artifacts:
    - path: scripts/test-convention-contracts.mjs
      provides: "Focused active-surface regression scanner"
    - path: scripts/test-convention-contracts.test.mjs
      provides: "Fail-first, clean-fixture, exclusion, and allowlist tests"
    - path: package.json
      provides: "test:convention-contracts command"
  key_links:
    - from: package.json
      to: scripts/test-convention-contracts.mjs
      via: "test:convention-contracts script"
      pattern: "test:convention-contracts"
---

<objective>
Create Wave 0 regression infrastructure for the clean-break convention migration.

Purpose: Make stale active surfaces and over-broad rewrites fail deterministically before producer/consumer changes.
Output: Executable scanner, scanner tests, and root command.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-RESEARCH.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-PATTERNS.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-VALIDATION.md
</context>

## Artifacts this phase produces

- `scripts/test-convention-contracts.mjs`
- `scripts/test-convention-contracts.test.mjs`
- `pnpm test:convention-contracts`

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Build the active-surface guard with fail-first fixtures</name>
  <read_first>
    - .planning/phases/161-ad-hoc-convention-package-contracts/161-RESEARCH.md
    - .planning/phases/161-ad-hoc-convention-package-contracts/161-PATTERNS.md
    - scripts/test-onboarding-contracts.mjs
    - package.json
  </read_first>
  <files>scripts/test-convention-contracts.mjs, scripts/test-convention-contracts.test.mjs</files>
  <behavior>
    - A synthetic active file containing retired intent-contract, numbered identifier, or archetype-constraint vocabulary makes the scanner fail.
    - A clean fixture with current convention fields passes.
    - CHANGELOG.md and archived .planning fixtures are excluded.
    - WebRTC/URL protocol fields, workspace dependency schemes, generic NIP-5D/NAP prose, and Nostr event kinds pass.
    - Legacy literals are allowed only in the exact Vite/CLI negative-test cases that prove rejection.
  </behavior>
  <action>Implement a Node ESM scanner over explicit active roots from the research inventory, with path-aware rules rather than a repository-wide word replacement. Match removed intent-contract identifiers, numbered convention forms, old slug-plus-number examples, and archetype event-kind constraints. Exclude changelogs and archived planning; do not traverse the root `skills` symlink independently. Encode exact allowlists for unrelated protocol/kind uses and the dedicated rejection tests. Test violation, clean, exclusion, and allowlist fixtures before running against the dirty pre-migration repository; the fixture tests must be green even while the live scan is expected red.</action>
  <acceptance_criteria>
    - The violation fixture fails for every retired vocabulary family.
    - Clean and legitimate-use fixtures pass.
    - Historical exclusions and negative-test allowlists are explicit, narrow, and tested.
  </acceptance_criteria>
  <verify><automated>node --test scripts/test-convention-contracts.test.mjs</automated></verify>
  <done>The guard itself is proven content-sensitive and ready to gate all downstream plans.</done>
</task>

<task type="auto">
  <name>Task 2: Wire the guard into the root verification surface</name>
  <read_first>
    - package.json
    - scripts/test-convention-contracts.mjs
    - scripts/test-convention-contracts.test.mjs
  </read_first>
  <files>package.json</files>
  <action>Add `test:convention-contracts` using the existing root script naming style. The command must run scanner self-tests and then the live active-tree scan, propagate non-zero exits, and require no new dependency. Do not add it to unrelated install/build scripts or inspect the pre-existing `packages/cli/dist-bin` and `workshop` trees.</action>
  <acceptance_criteria>
    - The root command executes both scanner tests and the live scan.
    - A live stale surface fails the command; later clean plans make it green.
    - No dependency or unrelated workflow changes are introduced.
  </acceptance_criteria>
  <verify><automated>node --test scripts/test-convention-contracts.test.mjs &amp;&amp; node -e "const p=require('./package.json'); if(!p.scripts['test:convention-contracts']) process.exit(1)"</automated></verify>
  <done>Every implementation plan can invoke one stable active-contract guard.</done>
</task>

</tasks>

<threat_model>
ASVS level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository files → regression scanner | Untrusted source text and symlinks are classified by path and content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-16 | Tampering | active-surface scanner | high | mitigate | Fail-first and clean control fixtures prove the guard reacts to content rather than setup errors. |
| T-161-17 | Denial of Service | scanner scope | medium | mitigate | Explicit roots/exclusions avoid recursive history and symlink traversal. |
| T-161-18 | Repudiation | allowlist | medium | mitigate | Path-aware allowlist entries are fixture-tested and limited to known legitimate uses. |
</threat_model>

<verification>
Run scanner fixtures in Wave 0; the live scan becomes a required green gate after all migration plans.
</verification>

<success_criteria>
Wave 0 supplies a deterministic, narrow, fail-first guard without changing production behavior.
</success_criteria>

<output>
Create `.planning/phases/161-ad-hoc-convention-package-contracts/161-06-SUMMARY.md` when done.
</output>

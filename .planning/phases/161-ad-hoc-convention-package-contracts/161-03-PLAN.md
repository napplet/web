---
phase: 161-ad-hoc-convention-package-contracts
plan: "03"
type: execute
wave: 1
depends_on: ["161-06"]
files_modified:
  - packages/vite-plugin/src/types.ts
  - packages/vite-plugin/src/manifest.ts
  - packages/vite-plugin/src/index.test.ts
autonomous: true
requirements:
  - CONV-PKG-02
must_haves:
  truths:
    - "A configured archetype emits one truthful three-element tag containing slug and one opaque convention."
    - "Numbered identifiers are rejected at the Vite authoring boundary."
    - "Archetype metadata never emits payload or event-kind constraints and remains excluded from aggregateHash folding."
  artifacts:
    - path: packages/vite-plugin/src/types.ts
      provides: "Convention-only archetype option shape"
    - path: packages/vite-plugin/src/manifest.ts
      provides: "Three-element archetype tag emission"
    - path: packages/vite-plugin/src/index.test.ts
      provides: "Negative legacy-input and exact-tag/hash regression coverage"
  key_links:
    - from: packages/vite-plugin/src/types.ts
      to: packages/vite-plugin/src/manifest.ts
      via: "Nip5aManifestOptions.archetypes"
      pattern: "archetypes"
    - from: packages/vite-plugin/src/manifest.ts
      to: packages/vite-plugin/src/index.test.ts
      via: "signed manifest event tags"
      pattern: "\\['archetype'"
---

<objective>
Migrate the Vite manifest producer to one opaque convention per archetype tag.

Purpose: Ensure build tooling advertises only metadata defined by the pinned upstream archetype documents.
Output: Updated option types, manifest serialization, negative tests, and package documentation.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-RESEARCH.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-PATTERNS.md
</context>

## Artifacts this phase produces

- Convention-only `Nip5aManifestOptions.archetypes` member
- `["archetype", slug, convention]` manifest tags
- Vite legacy-identifier and constraint-removal tests

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one convention from Vite config into signed manifest metadata</name>
  <reversibility rating="costly">The option and emitted public manifest shape are published contracts; undoing this upstream-locked change would require coordinated consumer changes.</reversibility>
  <read_first>
    - https://github.com/napplet/naps/blob/6461e4b37c29dc09a20dff35d9515889c4433874/ARCHETYPES.md
    - https://github.com/napplet/naps/blob/6461e4b37c29dc09a20dff35d9515889c4433874/CONVENTION-TEMPLATE.md
    - packages/vite-plugin/src/types.ts
    - packages/vite-plugin/src/manifest.ts
    - packages/vite-plugin/src/index.test.ts
  </read_first>
  <files>packages/vite-plugin/src/types.ts, packages/vite-plugin/src/manifest.ts, packages/vite-plugin/src/index.test.ts</files>
  <behavior>
    - Test 1: note plus napplet:note/open emits exactly one three-string archetype tag in that order.
    - Test 2: a numbered NAP identifier is rejected and no manifest is emitted from it.
    - Test 3: no event-kind constraint token is generated from any archetype input.
    - Test 4: archetype tags remain outside the aggregateHash path-tag fold.
  </behavior>
  <action>Write the negative and exact-output cases first, then replace the existing `naps`/contract option branches in `Nip5aManifestOptions.archetypes` and `buildArchetypeTags` with one required opaque convention for each object-form archetype. Preserve the existing slug/required-field validation and only trim boundary whitespace already normalized by the producer. Reject the removed numbered form explicitly. Emit precisely role, slug, and convention; remove constraint parsing/emission. Do not parse query components, canonicalize convention strings, normalize Unicode, aggregate multiple conventions into a new tag shape, or infer payload/event-kind semantics. The multi-convention encoding ambiguity tracked in web#183 remains a non-goal.</action>
  <acceptance_criteria>
    - The manifest contains exactly the documented three-string tag for the happy path.
    - Legacy numbered input is a tested error.
    - No constraint token is emitted.
    - Existing aggregateHash exclusion tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @napplet/vite-plugin test:unit &amp;&amp; pnpm --filter @napplet/vite-plugin type-check</automated>
  </verify>
  <done>A real Vite config produces truthful current archetype metadata and the removed inputs fail closed.</done>
</task>

</tasks>

<threat_model>
ASVS level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| author config → signed manifest | Untrusted build configuration becomes public, signed discovery metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-07 | Spoofing | archetype manifest tags | high | mitigate | Emit only the upstream three-element role/slug/convention shape and test exact output. |
| T-161-08 | Tampering | convention input validation | medium | mitigate | Reject the removed numbered form while preserving opaque strings without semantic parsing. |
| T-161-09 | Tampering | aggregateHash construction | high | mitigate | Preserve existing tests proving archetype metadata does not enter the path-tag hash fold. |
</threat_model>

<verification>
Run Vite plugin unit tests, type-check, and build; inspect the generated event tags through the existing test harness.
</verification>

<success_criteria>
Vite accepts current convention metadata, rejects the removed model, emits no invented constraint fields, and preserves aggregate hashing.
</success_criteria>

<output>
Create `.planning/phases/161-ad-hoc-convention-package-contracts/161-03-SUMMARY.md` when done.
</output>

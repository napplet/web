---
phase: 161-ad-hoc-convention-package-contracts
plan: "02"
type: execute
wave: 1
depends_on: ["161-06"]
files_modified:
  - packages/core/src/topics.ts
  - packages/nap/src/inc/types.ts
  - packages/nap/src/inc/shim.ts
  - packages/nap/src/inc-compat.test.ts
  - packages/nap/src/boundary-smoke.test.ts
autonomous: true
requirements:
  - CONV-PKG-04
must_haves:
  truths:
    - "INC subscriptions and deliveries compare complete topic strings exactly."
    - "Standard constants and examples use the advisory napplet:<archetype>/<intent> namespace."
    - "A query-bearing, prefixed, wildcard-like, or canonically equivalent string never matches a different subscribed string."
  artifacts:
    - path: packages/core/src/topics.ts
      provides: "Current advisory convention topic constants"
    - path: packages/nap/src/inc/shim.ts
      provides: "Exact Map-key topic dispatch"
    - path: packages/nap/src/inc-compat.test.ts
      provides: "No-normalization and exact-topic regression coverage"
  key_links:
    - from: packages/nap/src/inc/types.ts
      to: packages/nap/src/inc/shim.ts
      via: "opaque string topic field"
      pattern: "topic: string"
    - from: packages/nap/src/inc/shim.ts
      to: packages/nap/src/inc-compat.test.ts
      via: "direct handler-map lookup"
      pattern: "get\\(msg\\.topic\\)"
---

<objective>
Move INC constants and examples to convention namespaces while proving routing remains exact-string only.

Purpose: Prevent the upstream example ambiguity from becoming locally invented wildcard, prefix, query, or normalization behavior.
Output: Updated topic constants/types/examples and permanent exact-routing regression tests.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-RESEARCH.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-PATTERNS.md
</context>

## Artifacts this phase produces

- Advisory `napplet:note/open`, `napplet:profile/open`, and `napplet:dm/open` constants/examples
- Exact-topic negative regression cases

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove exact topic identity through a real INC subscription and delivery</name>
  <read_first>
    - https://github.com/napplet/naps/blob/6461e4b37c29dc09a20dff35d9515889c4433874/naps/NAP-INC.md
    - packages/nap/src/inc/shim.ts
    - packages/nap/src/inc-compat.test.ts
    - packages/nap/src/boundary-smoke.test.ts
  </read_first>
  <files>packages/nap/src/inc/shim.ts, packages/nap/src/inc-compat.test.ts, packages/nap/src/boundary-smoke.test.ts</files>
  <behavior>
    - Test 1: an event delivered on the subscribed byte-identical topic invokes the handler once.
    - Test 2: strings that differ only by query text, prefix/suffix text, wildcard-like characters, case, or Unicode composition do not invoke that handler.
    - Test 3: payload and sender values retain existing defaulting and delivery behavior.
  </behavior>
  <action>Write the exact-topic cases first, including the unresolved query and Unicode edges as inequality cases rather than normalization rules, and confirm the new cases fail if the dispatcher is temporarily changed to prefix matching. Retain direct map-key lookup of the complete topic string and the existing sender/payload forwarding. Do not split, decode, canonicalize, case-fold, wildcard-match, prefix-match, or query-parse any topic. Empty/null convention input remains governed only by existing required-field behavior; do not mint new semantics.</action>
  <acceptance_criteria>
    - Only a byte-identical topic key reaches the subscribed handler.
    - Query, prefix, wildcard-like, case, and Unicode variants remain distinct opaque strings.
    - Existing payload and sender behavior stays covered.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @napplet/nap test:unit -- inc-compat.test.ts boundary-smoke.test.ts</automated>
  </verify>
  <done>A production INC path delivers the exact subscribed topic and rejects every normalization/prefix probe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace active standard-topic constants and examples with convention namespaces</name>
  <read_first>
    - https://github.com/napplet/naps/blob/6461e4b37c29dc09a20dff35d9515889c4433874/ARCHETYPES.md
    - https://github.com/napplet/naps/blob/6461e4b37c29dc09a20dff35d9515889c4433874/naps/NAP-INC.md
    - packages/core/src/topics.ts
    - packages/nap/src/inc/types.ts
    - packages/nap/src/boundary-smoke.test.ts
  </read_first>
  <files>packages/core/src/topics.ts, packages/nap/src/inc/types.ts, packages/nap/src/boundary-smoke.test.ts</files>
  <behavior>
    - Test 1: exported examples/constants use the three pinned advisory convention values.
    - Test 2: type examples describe topics as opaque strings rather than a finite payload protocol.
    - Test 3: package boundary smoke coverage compiles and routes the new values.
  </behavior>
  <action>Update the existing exported topic constants, JSDoc, and boundary fixtures to the upstream archetype/intent namespace examples. Keep names directional and payload examples explicitly local. Do not revive removed payload shapes or infer a relationship between a convention name and a Nostr event kind. Preserve all unrelated core protocol and event-kind constants.</action>
  <acceptance_criteria>
    - Active constants/examples use note/open, profile/open, and dm/open convention names.
    - No payload schema or event-kind constraint is attached to those names.
    - Unrelated protocol and event-kind constants are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @napplet/core type-check &amp;&amp; pnpm --filter @napplet/nap test:unit &amp;&amp; pnpm --filter @napplet/nap type-check</automated>
  </verify>
  <done>INC-facing code teaches current convention namespaces and still routes raw topic strings exactly.</done>
</task>

</tasks>

<threat_model>
ASVS level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shell → napplet INC event | An untrusted topic, sender identifier, and opaque payload enter the client handler registry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-04 | Tampering | INC topic dispatcher | high | mitigate | Retain direct exact-key lookup and add negative cases for every proposed normalization/prefix behavior. |
| T-161-05 | Spoofing | convention topic examples | medium | mitigate | Use only pinned upstream advisory names and make no payload or sender claim from the topic text. |
| T-161-06 | Tampering | INC payload | medium | mitigate | Continue treating payload as opaque; receiving handlers own validation for their actual local convention. |
</threat_model>

<verification>
Run the focused INC regression tests, then core/NAP type-check and all NAP unit tests.
</verification>

<success_criteria>
INC code and examples use current convention names, and exact byte-string identity remains the sole matching rule.
</success_criteria>

<output>
Create `.planning/phases/161-ad-hoc-convention-package-contracts/161-02-SUMMARY.md` when done.
</output>

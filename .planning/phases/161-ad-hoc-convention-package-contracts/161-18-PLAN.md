---
phase: 161-ad-hoc-convention-package-contracts
plan: "18"
type: execute
wave: 9
depends_on: ["161-17"]
files_modified:
  - packages/nap/src/intent/sdk.ts
  - packages/nap/src/intent/index.ts
  - packages/sdk/src/cvm.ts
  - packages/sdk/src/nap-runtime.ts
  - packages/sdk/src/nap-types.ts
autonomous: true
requirements: [CONV-PKG-01]
must_haves:
  truths:
    - "NAP subpath and SDK consumers receive identical URI invoke/open and onDelivery signatures."
    - "Every adopted contract/delivery/envelope type is available from intended public barrels."
  artifacts:
    - path: packages/nap/src/intent/index.ts
      provides: "NAP intent public exports"
    - path: packages/sdk/src/cvm.ts
      provides: "Named SDK intent facade"
  key_links:
    - from: packages/sdk/src/cvm.ts
      to: packages/core/src/types/global/service-api.ts
      via: "SdkDomain<'intent'> delegation"
      pattern: "requireDomain\\('intent'\\)"
---

<objective>
Propagate the adopted intent contract through the NAP subpath and SDK facade.

Purpose: Keep ergonomic aliases and existing import paths aligned with the core/runtime contract.
Output: Public exports, URI helpers, delivery subscription helpers, declarations, and current JSDoc.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-17-SUMMARY.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Carry one URI invocation and delivery listener through every public facade</name>
  <files>packages/nap/src/intent/sdk.ts, packages/nap/src/intent/index.ts, packages/sdk/src/cvm.ts, packages/sdk/src/nap-runtime.ts, packages/sdk/src/nap-types.ts</files>
  <action>Per D-04/D-05, update `intentInvoke`, `intentOpen`, the `intent` SDK object, named runtime re-exports, and type barrels to URI/options and `onDelivery`. Export IntentInvokeOptions, IntentContract, IntentDelivery, IntentDeliveryMessage, and the updated unions. Forward inputs/callbacks unchanged to the injected domain; do not normalize twice, select/authorize handlers, inspect payloads/kinds, add INC fallback, or describe successful acceptance as completed handling.</action>
  <verify><automated>pnpm --filter @napplet/nap build &amp;&amp; pnpm --filter @napplet/nap type-check &amp;&amp; pnpm --filter @napplet/sdk build &amp;&amp; pnpm --filter @napplet/sdk type-check</automated></verify>
  <done>All intended NAP and SDK import paths compile with the same adopted URI, contract, acceptance, and delivery surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| SDK facade → injected runtime domain | Thin wrappers must not become a second policy or normalization authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-49 | Elevation of Privilege | SDK helper | high | mitigate | Helpers delegate unchanged and contain no handler selection or authorization logic. |
</threat_model>

<verification>
Build and type-check NAP and SDK declarations against the core source contract.
</verification>

<success_criteria>
Public NAP/SDK surfaces are complete, consistent, and policy-neutral.
</success_criteria>

<output>
Create `.planning/phases/161-ad-hoc-convention-package-contracts/161-18-SUMMARY.md` when done.
</output>

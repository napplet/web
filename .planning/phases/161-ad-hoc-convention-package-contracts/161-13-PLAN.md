---
phase: 161-ad-hoc-convention-package-contracts
plan: "13"
type: execute
wave: 4
depends_on: ["161-02", "161-06", "161-07"]
files_modified:
  - packages/core/src/types/global/nostr-api.ts
  - packages/nap/src/inc/types.ts
  - packages/nap/src/inc/shim.ts
  - packages/nap/src/inc/sdk.ts
  - packages/nap/src/inc-compat.test.ts
  - packages/sdk/src/relay.ts
autonomous: true
requirements: [CONV-PKG-04]
must_haves:
  truths:
    - "Per D-01, every public INC surface clean-breaks to emit(topic, payload?) with no extraTags/content overload."
    - "A queried napplet convention URI emits its queryless stable topic plus a shallow percent-decoded text payload before exact routing."
    - "Literal plus remains plus; fragments, malformed percent encoding, repeated decoded names, and query plus explicit payload throw before postMessage."
    - "Queryless payloads remain opaque and subscriptions/shell-delivered topics retain exact complete-string identity."
  artifacts:
    - path: packages/core/src/types/global/nostr-api.ts
      provides: "Canonical IncApi.emit(topic, payload?) public signature"
    - path: packages/nap/src/inc/shim.ts
      provides: "Runtime convention-URI transposition before postToShell"
    - path: packages/nap/src/inc-compat.test.ts
      provides: "Happy-path, literal-plus, decoding, rejection, and exact-routing coverage"
    - path: packages/sdk/src/relay.ts
      provides: "Named SDK inc.emit(topic, payload?) forwarding"
  key_links:
    - from: packages/sdk/src/relay.ts
      to: packages/core/src/types/global/nostr-api.ts
      via: "SdkDomain<'inc'> satisfies the canonical public interface"
      pattern: "emit\\(topic: string, payload\\?: unknown\\)"
    - from: packages/nap/src/inc/shim.ts
      to: packages/nap/src/inc-compat.test.ts
      via: "window.parent.postMessage envelope assertions before exact receive routing"
      pattern: "inc\\.emit"
---

<objective>
Implement D-01 from NAP-INC draft PR #89 head `34ec29fc4039384a83dbd6b476f83c4fa0d038e6`: clean-break the runtime/public emit API and transpose convention-URI queries before exact routing.

Purpose: Replace the repository's legacy Nostr-event-shaped emit arguments with the canonical topic/payload contract without moving query behavior into NAP-INTENT, manifests, subscriptions, or shell matching.
Output: Core, NAP, and SDK signatures; runtime transposition; strict fail-before-emission behavior; permanent tests.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-RESEARCH.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-02-SUMMARY.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-07-SUMMARY.md
</context>

## Artifacts this phase produces

- `IncApi.emit(topic: string, payload?: unknown): void`
- `emit(topic: string, payload?: unknown): void`
- `incEmit(topic: string, payload?: unknown): void`
- Internal `transposeConventionUri`-equivalent preprocessing seam
- PR #89 convention-URI transposition and rejection regression cases

## Source Coverage Audit

| Source | ID | Feature/requirement | Plan | Status |
|--------|----|---------------------|------|--------|
| GOAL | — | PR #89 NAP-INC query transposition before exact routing | 161-13 | COVERED |
| REQ | CONV-PKG-04 | Clean-break emit, transposition, strict rejects, exact routing | 161-13 | COVERED |
| RESEARCH | — | Avoid form decoding and legacy overloads | 161-13 | COVERED |
| CONTEXT | D-01 | Exact-head NAP-INC behavior; NAP-INTENT remains opaque | 161-13 | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one convention URI through the clean-break emit API to its wire envelope</name>
  <reversibility rating="costly">This changes published core, NAP, and SDK call signatures; undo requires coordinated downstream call-site migration, but D-01 and the cited draft already choose the clean break.</reversibility>
  <read_first>
    - https://github.com/napplet/naps/blob/34ec29fc4039384a83dbd6b476f83c4fa0d038e6/naps/NAP-INC.md
    - packages/core/src/types/global/nostr-api.ts
    - packages/nap/src/inc/shim.ts
    - packages/nap/src/inc/sdk.ts
    - packages/nap/src/inc-compat.test.ts
  </read_first>
  <files>packages/core/src/types/global/nostr-api.ts, packages/nap/src/inc/shim.ts, packages/nap/src/inc/sdk.ts, packages/nap/src/inc-compat.test.ts</files>
  <behavior>
    - `emit('napplet:profile/open?pubkey=abc%20123&amp;marker=a+b')` posts `{ type: 'inc.emit', topic: 'napplet:profile/open', payload: { pubkey: 'abc 123', marker: 'a+b' } }`.
    - A queryless topic with an object, string, array, primitive, or null payload forwards that payload unchanged.
    - Core declarations, the shim export, and `incEmit` accept only topic plus optional unknown payload.
  </behavior>
  <action>Write fail-first postMessage assertions, then change `IncApi.emit`, the NAP shim `emit`, and `incEmit` to `(topic: string, payload?: unknown)`. Remove the ignored `extraTags` argument, JSON-string `content` parsing, and all overload/compatibility branches rather than detecting the old array/string call form. In the shim, preprocess only a queried `napplet:<archetype>/<intent>` convention URI: separate the path as the stable topic, split the raw query into name/value pairs, percent-decode names and values as text with `decodeURIComponent`, preserve `+` literally, build a plain shallow text map, and call `postToShell` once with the transformed envelope. For queryless topics, forward the caller's payload unchanged. Do not parse NAP-INTENT convention fields, manifest metadata, subscriptions, or inbound events per D-01.</action>
  <acceptance_criteria>
    - The happy-path convention URI produces the exact stable-topic/payload envelope before routing.
    - Queryless opaque payloads are not JSON-parsed, coerced, cloned, or discarded.
    - No public declaration or runtime branch retains the legacy three-argument API.
  </acceptance_criteria>
  <verify><automated>pnpm --filter @napplet/nap test:unit -- inc-compat.test.ts &amp;&amp; pnpm --filter @napplet/core type-check &amp;&amp; pnpm --filter @napplet/nap type-check</automated></verify>
  <done>A real public call reaches postMessage with the PR #89 stable topic and transposed text payload through the clean-break signature.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the complete rejection matrix and propagate the SDK signature</name>
  <read_first>
    - https://github.com/napplet/naps/blob/34ec29fc4039384a83dbd6b476f83c4fa0d038e6/naps/NAP-INC.md
    - packages/nap/src/inc/types.ts
    - packages/nap/src/inc/shim.ts
    - packages/nap/src/inc-compat.test.ts
    - packages/sdk/src/relay.ts
  </read_first>
  <files>packages/nap/src/inc/types.ts, packages/nap/src/inc/shim.ts, packages/nap/src/inc-compat.test.ts, packages/sdk/src/relay.ts</files>
  <behavior>
    - A fragment in a convention URI throws synchronously and posts no envelope.
    - Malformed percent escapes in a query name or value throw synchronously and post no envelope.
    - Repeated raw names and names that become equal only after decoding both reject without last-write-wins.
    - A queried convention URI plus a defined explicit payload rejects before emission.
    - `%2B` and literal `+` both decode to plus, never a space; values remain strings with no boolean/number/null coercion.
    - Existing query/prefix/wildcard/case/Unicode receive-side non-match tests remain green.
  </behavior>
  <action>Complete the D-01 validator in the runtime seam and test each rejection through the exported `emit`; clear the postMessage mock between cases and assert zero calls on every failure. Detect duplicate names after percent decoding so `%61=one&amp;a=two` rejects. Do not use `URLSearchParams`, because it applies form-urlencoded plus-to-space semantics. Update `IncEmitMessage`/`IncEventMessage` JSDoc to distinguish developer-facing convention URI input from the stable wire topic, and update `packages/sdk/src/relay.ts` so `inc.emit(topic, payload?)` delegates without transformation. Preserve the direct `Map.get(msg.topic)` inbound path and the deprecated IFC aliases' reference identity; their re-exports inherit the clean-break signature without a compatibility overload.</action>
  <acceptance_criteria>
    - Every draft-specified invalid form throws before postMessage.
    - Literal-plus, percent-decoding, decoded-duplicate, and no-coercion cases are asserted exactly.
    - Built core/NAP/SDK declarations expose topic plus optional unknown payload only.
    - Receive-side exact routing behavior from completed Plan 161-02 is unchanged.
  </acceptance_criteria>
  <verify><automated>pnpm --filter @napplet/nap test:unit -- inc-compat.test.ts &amp;&amp; pnpm --filter @napplet/core build &amp;&amp; pnpm --filter @napplet/nap build &amp;&amp; pnpm --filter @napplet/sdk build &amp;&amp; pnpm --filter @napplet/sdk type-check</automated></verify>
  <done>The full transposition/rejection contract is enforced at emit time, exported consistently, and leaves exact routing intact.</done>
</task>

</tasks>

<threat_model>
ASVS level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| napplet call → runtime emit | Untrusted convention URI and payload enter preprocessing before postMessage. |
| runtime envelope → shell router | The transformed stable topic and opaque payload cross into exact shell routing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-37 | Tampering | query map construction | high | mitigate | Percent-decode once, reject decoded duplicate names, preserve literal plus, and assert exact envelopes. |
| T-161-38 | Tampering | topic/payload ambiguity | high | mitigate | Reject fragments and query-plus-payload before postMessage; keep transform confined to emit. |
| T-161-39 | Spoofing | exact topic identity | high | mitigate | Strip only the convention query before emission and retain direct exact-key receive routing with existing negative tests. |
</threat_model>

<verification>
Run focused NAP tests first, then build/type-check core, NAP, and SDK declarations; no new package or external setup is involved.
</verification>

<success_criteria>
The runtime and every public wrapper implement PR #89's convention-URI sugar exactly, reject every specified invalid form before emission, and preserve exact topic routing.
</success_criteria>

<output>
Create `.planning/phases/161-ad-hoc-convention-package-contracts/161-13-SUMMARY.md` when done.
</output>

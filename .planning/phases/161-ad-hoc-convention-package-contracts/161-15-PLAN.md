---
phase: 161-ad-hoc-convention-package-contracts
plan: "15"
type: execute
wave: 7
depends_on: ["161-13", "161-14"]
files_modified:
  - packages/nap/src/convention-uri.ts
  - packages/nap/src/convention-uri.test.ts
  - packages/nap/src/inc/shim.ts
  - packages/nap/src/inc-compat.test.ts
autonomous: true
requirements: [CONV-PKG-01, CONV-PKG-04]
must_haves:
  truths:
    - "INC emit and intent invoke/open can share one exact convention-URI syntax and rejection implementation per D-03."
    - "INC receive routing remains exact, queryless, and independent of intent delivery."
  artifacts:
    - path: packages/nap/src/convention-uri.ts
      provides: "Internal query transposition and authoritative identity derivation"
    - path: packages/nap/src/convention-uri.test.ts
      provides: "Cross-operation normalization matrix"
  key_links:
    - from: packages/nap/src/inc/shim.ts
      to: packages/nap/src/convention-uri.ts
      via: "queried convention preprocessing before postToShell"
      pattern: "normalizeConventionUri"
---

<objective>
Extract the adopted convention-URI algorithm into one internal primitive without broadening where URI interpretation occurs.

Purpose: PR #89 head `4593ce9e301ce098fd3dad64206fcd6f144fa7af`, PR #90 head `896c32c92deee68dc4d10fc1132b62df20cccb6f`, and PR #91 head `a718915ddefa2f03a0126579601f59d8bd86f7c4` share syntax and rejection rules.
Output: A tested normalizer and unchanged INC wire/routing behavior.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-PR89-91-DELTA.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-13-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one queried convention through the shared primitive and INC envelope</name>
  <files>packages/nap/src/convention-uri.ts, packages/nap/src/convention-uri.test.ts, packages/nap/src/inc/shim.ts, packages/nap/src/inc-compat.test.ts</files>
  <behavior>
    - A queryless URI derives archetype, action, stable convention, and preserves an explicit opaque payload.
    - A queried URI yields a shallow decoded text map, keeps literal plus, and rejects fragments, malformed escapes, repeated decoded names, or explicit payload.
    - INC posts the same stable `inc.emit` envelope as before and inbound topic lookup stays exact.
  </behavior>
  <action>Per D-02/D-03, create an internal `normalizeConventionUri(uri, explicitPayload?)` returning URI-derived `archetype`, `action`, queryless `convention`, and optional normalized `payload`. Move raw pair splitting/`decodeURIComponent` behavior out of INC, then have INC invoke it only for documented-form `napplet:` inputs and continue forwarding all other opaque topics unchanged. Do not use form decoding, normalize case/Unicode, parse payload semantics, change subscription matching, export the helper as public API, or make intent depend on INC.</action>
  <verify><automated>pnpm --filter @napplet/nap test:unit -- convention-uri.test.ts inc-compat.test.ts &amp;&amp; pnpm --filter @napplet/nap type-check</automated></verify>
  <done>The shared helper passes the complete adopted matrix and the pre-existing exact INC routing suite remains green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| developer URI → postMessage envelope | Untrusted URI text is interpreted before entering a runtime carrier. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-43 | Tampering | convention normalizer | high | mitigate | Table-test every ambiguity rejection before postMessage and preserve literal-plus semantics. |
| T-161-44 | Elevation of Privilege | operation scope | medium | mitigate | Keep the helper internal and call it only from the two adopted operations; exact receive routing is regression-tested. |
</threat_model>

<verification>
Run the shared helper and real INC postMessage suites, then NAP type-check.
</verification>

<success_criteria>
One internal algorithm serves only the adopted URI boundaries and changes no receive-side or payload-schema semantics.
</success_criteria>

<output>
Create `.planning/phases/161-ad-hoc-convention-package-contracts/161-15-SUMMARY.md` when done.
</output>

---
phase: 161-ad-hoc-convention-package-contracts
plan: "01"
type: execute
wave: 1
depends_on: ["161-06"]
files_modified:
  - packages/core/src/types/intent.ts
  - packages/core/src/index.ts
  - packages/core/src/types/global.ts
  - packages/core/src/types/global/service-api.ts
  - packages/nap/src/intent/types.ts
  - packages/nap/src/intent/index.ts
  - packages/nap/src/intent/shim.ts
  - packages/nap/src/intent/sdk.ts
  - packages/nap/src/intent/shim.test.ts
autonomous: true
requirements:
  - CONV-PKG-01
must_haves:
  truths:
    - "NAP-INTENT callers request an optional opaque convention and receive candidates that advertise opaque conventions."
    - "The public core and intent subpath contain no compatibility alias for the removed numbered contract model."
    - "Intent payloads remain unknown data transported through the existing request/result correlation path."
  artifacts:
    - path: packages/core/src/types/intent.ts
      provides: "Canonical IntentRequest.convention, IntentCandidate.conventions, and IntentResult.convention fields"
    - path: packages/nap/src/intent/types.ts
      provides: "NAP-INTENT envelope types aligned with the canonical upstream contract"
    - path: packages/nap/src/intent/shim.test.ts
      provides: "Runtime regression coverage for opaque convention forwarding"
  key_links:
    - from: packages/core/src/types/intent.ts
      to: packages/nap/src/intent/types.ts
      via: "type-only imports and explicit barrel re-exports"
      pattern: "Intent(Request|Candidate|Result)"
    - from: packages/nap/src/intent/shim.ts
      to: packages/nap/src/intent/shim.test.ts
      via: "intent.invoke request/result correlation"
      pattern: "intent\\.invoke"
  prohibitions:
    - statement: "The migration must not invent protocol semantics beyond the pinned upstream NAP contract."
      status: unresolved
      verification: judgment
---

<objective>
Migrate the canonical core and `@napplet/nap/intent` public contract to unnumbered opaque conventions.

Purpose: Establish the type and runtime contract every downstream producer, fixture, document, and skill consumes.
Output: Convention-only intent interfaces, forwarding shims/SDK helpers, tests, and package documentation.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-RESEARCH.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-PATTERNS.md
</context>

## Artifacts this phase produces

- `IntentRequest.convention?: string`
- `IntentCandidate.conventions: string[]`
- `IntentResult.convention?: string`
- Convention-only NAP-INTENT exports and forwarding behavior

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one opaque convention through core types and an intent invocation</name>
  <reversibility rating="costly">This is a coordinated breaking public type migration across published packages, but the upstream source and phase context already lock the direction.</reversibility>
  <read_first>
    - https://github.com/napplet/naps/blob/6461e4b37c29dc09a20dff35d9515889c4433874/naps/NAP-INTENT.md
    - packages/core/src/types/intent.ts
    - packages/core/src/index.ts
    - packages/nap/src/intent/types.ts
    - packages/nap/src/intent/index.ts
    - packages/nap/src/intent/shim.test.ts
  </read_first>
  <files>packages/core/src/types/intent.ts, packages/core/src/index.ts, packages/nap/src/intent/types.ts, packages/nap/src/intent/index.ts, packages/nap/src/intent/shim.test.ts</files>
  <behavior>
    - Test 1: invoking the note/open convention posts that exact opaque string and resolves the same convention from the correlated result.
    - Test 2: candidate fixtures expose conventions and no contract-constraint collection.
    - Test 3: strict type-check accepts the three upstream convention fields from both public barrels.
  </behavior>
  <action>Start with the intent shim regression and public type assertions, confirm they fail against the current numbered model, then replace the source interfaces and explicit type exports with the three upstream convention fields. Delete `IntentContract` rather than aliasing it. Preserve archetype, action, payload, handler, behavior, candidate identity, default-selection fields, and existing envelope names. Treat convention values as byte-for-byte opaque strings; retain `payload?: unknown` and add no schema, parser, negotiation, compatibility, Unicode normalization, or empty/null semantics beyond the existing required-field behavior.</action>
  <acceptance_criteria>
    - The core and NAP intent barrels expose only the pinned upstream intent field names.
    - The shim test proves an opaque convention round-trips without transformation.
    - The removed contract type has no exported alias.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @napplet/core type-check &amp;&amp; pnpm --filter @napplet/nap test:unit -- shim.test.ts</automated>
  </verify>
  <done>The convention-only public type path compiles and one real intent invocation round-trips an unchanged convention.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate the convention contract through global APIs and runtime helpers</name>
  <read_first>
    - https://github.com/napplet/naps/blob/6461e4b37c29dc09a20dff35d9515889c4433874/naps/NAP-INTENT.md
    - packages/core/src/types/global.ts
    - packages/core/src/types/global/service-api.ts
    - packages/nap/src/intent/shim.ts
    - packages/nap/src/intent/sdk.ts
    - packages/nap/src/intent/shim.test.ts
  </read_first>
  <files>packages/core/src/types/global.ts, packages/core/src/types/global/service-api.ts, packages/nap/src/intent/shim.ts, packages/nap/src/intent/sdk.ts, packages/nap/src/intent/shim.test.ts</files>
  <behavior>
    - Test 1: invoke and open helpers forward convention values without selecting or interpreting handlers.
    - Test 2: available-handler results expose convention arrays while existing timeout and top-level error behavior stays intact.
    - Test 3: payload values remain opaque unknown data and resolved `ok: false` or `handled: false` results are not converted into transport exceptions.
  </behavior>
  <action>Update the global intent API typing, public JSDoc, runtime forwarding, and bare SDK helpers to consume the source contract from Task 1. Keep correlation IDs, pending maps, timeouts, `open()` sugar, availability/change callbacks, and top-level transport-error handling unchanged. Preserve the shell/runtime as the handler-resolution authority: no direct handler address, client-side authorization shortcut, convention negotiation, or payload validation belongs in these helpers. Extend the existing test file before implementation for forwarding, failure, and opaque payload cases.</action>
  <acceptance_criteria>
    - All intent-facing global and helper signatures compile against the convention-only source type.
    - Handler selection remains a shell/runtime responsibility.
    - Existing timeout, error, and unsuccessful-result behavior is regression-tested.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @napplet/core type-check &amp;&amp; pnpm --filter @napplet/nap test:unit &amp;&amp; pnpm --filter @napplet/nap type-check</automated>
  </verify>
  <done>Every active core/global/NAP intent API forwards the new contract without adding routing or validation semantics.</done>
</task>

</tasks>

<threat_model>
ASVS level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| napplet → shell intent API | Untrusted requests and opaque payloads cross into the authoritative handler resolver. |
| shell result → napplet helper | Candidate metadata and result payloads return across the postMessage boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-01 | Elevation of Privilege | intent invocation helpers | high | mitigate | Keep handler selection and authorization shell-side; tests prove helpers forward archetype/action/convention only and add no direct-address bypass. |
| T-161-02 | Tampering | opaque intent payloads | medium | mitigate | Preserve `unknown` payload typing and document receiver-owned validation against the selected convention without inventing a repository schema. |
| T-161-03 | Spoofing | candidate convention metadata | medium | mitigate | Source candidate fields from shell responses and expose them without compatibility synthesis or client-side claims. |
</threat_model>

<verification>
Run core and NAP build, type-check, and unit gates; inspect built declarations to confirm the convention-only exported surface.
</verification>

<success_criteria>
The canonical public types, runtime helpers, tests, and package docs agree on opaque conventions and retain the existing authorization/error boundaries.
</success_criteria>

<output>
Create `.planning/phases/161-ad-hoc-convention-package-contracts/161-01-SUMMARY.md` when done.
</output>

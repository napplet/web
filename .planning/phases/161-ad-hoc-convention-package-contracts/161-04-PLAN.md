---
phase: 161-ad-hoc-convention-package-contracts
plan: "04"
type: execute
wave: 1
depends_on: ["161-06"]
files_modified:
  - packages/cli/src/types.ts
  - packages/cli/src/config.ts
  - packages/cli/src/manifest-metadata.ts
  - packages/cli/src/init-wizard.ts
  - packages/cli/src/cli.ts
  - packages/cli/src/mod.ts
  - packages/cli/src/output.ts
  - packages/cli/tests/config_test.ts
  - packages/cli/tests/init_wizard_test.ts
  - packages/cli/tests/manifest_test.ts
autonomous: true
requirements: [CONV-PKG-03]
must_haves:
  truths:
    - "CLI config and prompts accept a slug plus one opaque convention."
    - "CLI manifest emission produces exactly three-element archetype tags."
    - "Numbered identifiers and event-kind constraints are rejected or omitted."
  artifacts:
    - path: packages/cli/src/config.ts
      provides: "Single convention parser/normalizer boundary"
    - path: packages/cli/src/manifest-metadata.ts
      provides: "Convention-only archetype tag merge/emission"
    - path: packages/cli/tests/manifest_test.ts
      provides: "Exact output and negative regression cases"
  key_links:
    - from: packages/cli/src/config.ts
      to: packages/cli/src/manifest-metadata.ts
      via: "normalized archetype convention config"
      pattern: "convention"
---

<objective>
Migrate CLI configuration, prompts, output, and manifest generation to opaque conventions.

Purpose: Keep the primary authoring workflow aligned with Vite and upstream metadata.
Output: Renamed CLI model/parser, three-element tag emission, prompts, and Deno tests.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-RESEARCH.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-PATTERNS.md
</context>

## Artifacts this phase produces

- `NappletArchetypeConvention`
- `parseArchetypeConvention()`
- Convention-only CLI config and manifest tags

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one CLI convention from parsing into persisted config</name>
  <reversibility rating="costly">The renamed config model is published CLI behavior and requires coordinated migration, already locked by the upstream source.</reversibility>
  <read_first>
    - https://github.com/napplet/naps/blob/6461e4b37c29dc09a20dff35d9515889c4433874/ARCHETYPES.md
    - packages/cli/src/types.ts
    - packages/cli/src/config.ts
    - packages/cli/tests/config_test.ts
  </read_first>
  <files>packages/cli/src/types.ts, packages/cli/src/config.ts, packages/cli/tests/config_test.ts</files>
  <behavior>
    - Valid slug plus napplet:note/open parses and persists without semantic transformation.
    - Numbered input is rejected with current convention guidance.
    - Existing missing/invalid required-field behavior remains unchanged.
  </behavior>
  <action>Write the Deno cases first, then rename the contract model and centralized parser/normalizer to convention terminology. Accept the documented opaque form, retain only existing boundary whitespace handling, and explicitly reject the removed numbered form. Add no query parsing, canonicalization, Unicode normalization, aliases, payload schema, or new empty/null rules.</action>
  <acceptance_criteria>
    - Config round-trips the exact convention string.
    - Numbered input is a tested error.
    - No compatibility field survives in the normalized config.
  </acceptance_criteria>
  <verify><automated>deno task --config packages/cli/deno.json test:unit --filter config &amp;&amp; deno task --config packages/cli/deno.json check</automated></verify>
  <done>The CLI has one convention-only config boundary with negative legacy coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit truthful CLI archetype metadata</name>
  <read_first>
    - https://github.com/napplet/naps/blob/6461e4b37c29dc09a20dff35d9515889c4433874/ARCHETYPES.md
    - packages/cli/src/manifest-metadata.ts
    - packages/cli/tests/manifest_test.ts
    - packages/cli/src/config.ts
  </read_first>
  <files>packages/cli/src/manifest-metadata.ts, packages/cli/tests/manifest_test.ts</files>
  <behavior>
    - Emits exactly role, slug, and convention.
    - Never emits an event-kind constraint.
    - Read/merge/dedupe and file-error behavior remain intact.
  </behavior>
  <action>Update `isCanonicalArchetypeTag` and manifest merge/emission to accept only three strings sourced from normalized config. Remove constraint recognition/emission and add exact negative/output tests. Preserve NotFound/SyntaxError handling, metadata precedence, deduplication, signing, and aggregate-hash behavior. Leave repeated/multi-value convention encoding unresolved per web#183.</action>
  <acceptance_criteria>
    - Exact three-element tags are emitted and recognized.
    - Constraint metadata cannot be emitted.
    - Existing metadata precedence and error tests pass.
  </acceptance_criteria>
  <verify><automated>deno task --config packages/cli/deno.json test:unit --filter manifest &amp;&amp; deno task --config packages/cli/deno.json check</automated></verify>
  <done>CLI deploy metadata is truthful and contains no retired constraint surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Carry convention terminology through prompts, commands, and output</name>
  <read_first>
    - packages/cli/src/init-wizard.ts
    - packages/cli/src/cli.ts
    - packages/cli/src/mod.ts
    - packages/cli/src/output.ts
    - packages/cli/tests/init_wizard_test.ts
    - packages/cli/src/config.ts
  </read_first>
  <files>packages/cli/src/init-wizard.ts, packages/cli/src/cli.ts, packages/cli/src/mod.ts, packages/cli/src/output.ts, packages/cli/tests/init_wizard_test.ts</files>
  <behavior>
    - Wizard prompt/help/output use slug plus convention examples.
    - Non-interactive flags flow through the centralized parser.
    - Unrelated URL/dependency protocol and Nostr event-kind wording remains unchanged.
  </behavior>
  <action>Update wizard questions, help text, public exports, completion output, and tests to the Task 1 model. Route all input through the centralized parser rather than duplicating validation. Preserve the CLI-first workflow and non-interactive behavior. Audit `output.ts` narrowly so unrelated URL and dependency-protocol wording is not renamed.</action>
  <acceptance_criteria>
    - Interactive and non-interactive flows produce the same convention config.
    - Public CLI exports use the new helper/type names.
    - Unrelated protocol vocabulary is preserved.
  </acceptance_criteria>
  <verify><automated>deno task --config packages/cli/deno.json test:unit &amp;&amp; deno task --config packages/cli/deno.json check</automated></verify>
  <done>Every CLI entry point teaches and persists the current convention contract.</done>
</task>

</tasks>

<threat_model>
ASVS level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user/automation → CLI config | Untrusted text enters persisted deployment metadata. |
| CLI config → signed manifest | Normalized author input becomes public discovery metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-10 | Spoofing | CLI archetype metadata | high | mitigate | Centralize validation and emit only the tested three-element tag. |
| T-161-11 | Tampering | convention parsing | medium | mitigate | Preserve opaque exact strings and reject only the documented removed form. |
| T-161-12 | Repudiation | CLI output/help | low | accept | Human-readable output is not an authority boundary; tests keep it aligned with persisted config. |
</threat_model>

<verification>
Run CLI Deno check and all unit tests, including numbered-input and metadata-shape cases.
</verification>

<success_criteria>
CLI input, persistence, prompts, output, exports, and manifest generation use one convention-only model.
</success_criteria>

<output>
Create `.planning/phases/161-ad-hoc-convention-package-contracts/161-04-SUMMARY.md` when done.
</output>

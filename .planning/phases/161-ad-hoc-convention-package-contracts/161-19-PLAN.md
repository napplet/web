---
phase: 161-ad-hoc-convention-package-contracts
plan: "19"
type: execute
wave: 9
depends_on: ["161-17"]
files_modified:
  - packages/shim/src/runtime.ts
  - packages/shim/src/shell.test.ts
autonomous: true
requirements: [CONV-PKG-01]
must_haves:
  truths:
    - "An injected intent domain exposes onDelivery and routes intent.deliver through the intent handler."
    - "Intent delivery remains available when the INC domain is not installed."
  artifacts:
    - path: packages/shim/src/runtime.ts
      provides: "Runtime injection and prefix routing for adopted intent APIs"
    - path: packages/shim/src/shell.test.ts
      provides: "Selected-domain injection regression coverage"
  key_links:
    - from: packages/shim/src/runtime.ts
      to: packages/nap/src/intent/shim.ts
      via: "handleIntentMessage and onDelivery imports"
      pattern: "intentOnDelivery"
---

<objective>
Expose and route the adopted delivery API through the thin runtime host.

Purpose: Ensure browser injection carries the NAP implementation without adding host-side protocol inventions.
Output: Runtime wiring and injection tests.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-17-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Inject onDelivery and route one push without INC</name>
  <files>packages/shim/src/runtime.ts, packages/shim/src/shell.test.ts</files>
  <behavior>
    - Selecting only the intent domain installs invoke/open/available/handlers/onChanged/onDelivery.
    - An intent.deliver message from the parent reaches the registered callback.
    - No inc domain is required or installed for this delivery.
  </behavior>
  <action>Import `onDelivery` from the NAP intent shim and expose it in `createNappletGlobal`; retain the existing `intent.` prefix router so `intent.deliver` enters `handleIntentMessage`. Extend runtime-injection tests with an intent-only install and a parent message carrying a no-ID delivery. Do not add a separate carrier, INC bridge, sender rewrite, lifecycle manager, or caller-visible queue controls.</action>
  <verify><automated>pnpm --filter @napplet/shim test:unit &amp;&amp; pnpm --filter @napplet/shim build &amp;&amp; pnpm --filter @napplet/shim type-check</automated></verify>
  <done>The injected intent domain receives delivery pushes independently of INC and exposes every adopted method.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| parent postMessage → injected domain | The web projection authenticates the parent carrier before dispatch. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-50 | Spoofing | runtime message listener | high | mitigate | Preserve the existing `event.source === window.parent` guard and test through that path. |
</threat_model>

<verification>
Run shim unit/build/type gates with an intent-only injected global.
</verification>

<success_criteria>
The runtime host remains thin and delivers the adopted no-ID push through the existing authenticated web carrier.
</success_criteria>

<output>
Create `.planning/phases/161-ad-hoc-convention-package-contracts/161-19-SUMMARY.md` when done.
</output>

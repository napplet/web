---
phase: 161-ad-hoc-convention-package-contracts
plan: "07"
type: execute
wave: 3
depends_on: ["161-01", "161-05", "161-06"]
files_modified:
  - packages/sdk/src/index.ts
  - packages/sdk/src/nap-runtime.ts
  - packages/sdk/src/nap-types.ts
  - packages/shim/src/index.ts
  - packages/shim/src/runtime.ts
  - packages/shim/src/prelude.ts
  - packages/shim/src/shell.test.ts
  - packages/conformance-cli/src/cli.ts
  - apps/conformance/index.html
  - apps/conformance/src/main.ts
autonomous: true
requirements: [CONV-PKG-01, CONV-PKG-05]
must_haves:
  truths:
    - "SDK and shim type propagation compiles with convention-only intent types and adds no negotiation behavior."
    - "Conformance CLI/web copy presents conventions without becoming a payload validator."
    - "No unrelated runtime transport or protocol field is removed during the audit."
  artifacts:
    - path: packages/sdk/src/nap-types.ts
      provides: "Convention-only type propagation"
    - path: packages/shim/src/runtime.ts
      provides: "Audited runtime host with no negotiation addition"
    - path: packages/conformance-cli/src/cli.ts
      provides: "Current conformance CLI copy"
  key_links:
    - from: packages/sdk/src/nap-types.ts
      to: packages/core/src/types/intent.ts
      via: "type-only import from Plan 161-01"
      pattern: "Intent"
---

<objective>
Audit downstream SDK/shim runtime surfaces and conformance presentation against the migrated contract.

Purpose: Close transitive type/copy gaps without inventing runtime negotiation or validation.
Output: Type-clean SDK/shim sources and current conformance CLI/web wording.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-RESEARCH.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-PATTERNS.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-01-SUMMARY.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-05-SUMMARY.md
</context>

## Artifacts this phase produces

- Convention-compatible SDK/shim type propagation
- Current conformance CLI/web labels and examples

<tasks>

<task type="tracer">
  <name>Task 1: Compile SDK type propagation against convention-only intent</name>
  <read_first>
    - packages/sdk/src/index.ts
    - packages/sdk/src/nap-runtime.ts
    - packages/sdk/src/nap-types.ts
    - .planning/phases/161-ad-hoc-convention-package-contracts/161-01-SUMMARY.md
  </read_first>
  <files>packages/sdk/src/index.ts, packages/sdk/src/nap-runtime.ts, packages/sdk/src/nap-types.ts</files>
  <action>Audit imports, re-exports, JSDoc, and runtime guards for the new intent types. Make only changes required by Plan 161-01; preserve optional-domain guards, named exports, and transport behavior. Add no convention parser, compatibility alias, payload schema, or handler-selection API. If the existing files are already clean, leave them byte-identical and record the verified no-change audit in the summary.</action>
  <acceptance_criteria>
    - SDK builds and type-checks against the convention-only core contract.
    - No runtime negotiation or compatibility surface is added.
    - A no-change result is explicitly recorded rather than forcing churn.
  </acceptance_criteria>
  <verify><automated>pnpm --filter @napplet/sdk build &amp;&amp; pnpm --filter @napplet/sdk type-check</automated></verify>
  <done>SDK propagation is verified current with no invented behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Audit shim hosting surfaces without changing transport</name>
  <read_first>
    - packages/shim/src/index.ts
    - packages/shim/src/runtime.ts
    - packages/shim/src/prelude.ts
    - packages/shim/src/shell.test.ts
    - .planning/phases/161-ad-hoc-convention-package-contracts/161-01-SUMMARY.md
  </read_first>
  <files>packages/shim/src/index.ts, packages/shim/src/runtime.ts, packages/shim/src/prelude.ts, packages/shim/src/shell.test.ts</files>
  <action>Audit intent type references, fixtures, and JSDoc for retired contract fields. Preserve runtime injection, selected-domain installation, postMessage transport, and unrelated tests. Do not reintroduce a handshake, negotiation layer, payload validator, or default convention. Leave clean files unchanged and document that result.</action>
  <acceptance_criteria>
    - Shim build, type-check, and unit tests pass with Plan 161-01.
    - Transport/injection behavior is unchanged.
    - No stale intent field remains in active shim code or fixtures.
  </acceptance_criteria>
  <verify><automated>pnpm --filter @napplet/shim build &amp;&amp; pnpm --filter @napplet/shim type-check &amp;&amp; pnpm --filter @napplet/shim test:unit</automated></verify>
  <done>Shim hosting is convention-compatible and transport-stable.</done>
</task>

<task type="auto">
  <name>Task 3: Align conformance CLI and web copy</name>
  <read_first>
    - packages/conformance-cli/src/cli.ts
    - apps/conformance/index.html
    - apps/conformance/src/main.ts
    - .planning/phases/161-ad-hoc-convention-package-contracts/161-05-SUMMARY.md
  </read_first>
  <files>packages/conformance-cli/src/cli.ts, apps/conformance/index.html, apps/conformance/src/main.ts</files>
  <action>Update active labels, examples, and diagnostics only where they teach the retired numbered contract. Preserve reporter formats, manifest-event resolution, URL input, boot behavior, Nostr event kinds, and generic protocol terminology. Do not claim the conformance UI validates convention payloads or query matching.</action>
  <acceptance_criteria>
    - CLI/web copy matches current conformance fixture behavior.
    - Existing conformance functionality remains unchanged.
    - No unspecified convention semantic is claimed.
  </acceptance_criteria>
  <verify><automated>pnpm --filter @napplet/conformance-cli build &amp;&amp; pnpm --filter @napplet/conformance-web build &amp;&amp; pnpm --filter @napplet/conformance-web type-check</automated></verify>
  <done>Conformance presentation uses current terminology without expanding validation.</done>
</task>

</tasks>

<threat_model>
ASVS level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core types → SDK/shim | Published contract crosses package boundaries. |
| conformance result → human | UI/CLI copy can misrepresent what was actually validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-19 | Elevation of Privilege | SDK/shim intent surface | high | mitigate | Preserve runtime-owned handler authorization and add no client resolver bypass. |
| T-161-20 | Spoofing | conformance copy | medium | mitigate | Copy describes carrier/metadata checks only and stays tied to tested runtime results. |
| T-161-21 | Tampering | opaque payload propagation | medium | mitigate | Keep payload unknown and unchanged across SDK/shim boundaries. |
</threat_model>

<verification>
Run focused SDK, shim, conformance CLI, and web build/type/unit gates.
</verification>

<success_criteria>
All downstream runtime and presentation surfaces are convention-compatible without new transport or validation semantics.
</success_criteria>

<output>
Create `.planning/phases/161-ad-hoc-convention-package-contracts/161-07-SUMMARY.md` when done.
</output>

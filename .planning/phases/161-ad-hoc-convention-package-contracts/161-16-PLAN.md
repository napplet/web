---
phase: 161-ad-hoc-convention-package-contracts
plan: "16"
type: execute
wave: 7
depends_on: ["161-01", "161-07"]
files_modified:
  - packages/core/src/types/intent.ts
  - packages/core/src/index.ts
  - packages/core/src/types/global/service-api.ts
  - packages/core/src/types/global.ts
  - packages/core/src/intent-contract.test.ts
autonomous: true
requirements: [CONV-PKG-01]
must_haves:
  truths:
    - "Public intent APIs accept an authoritative URI plus options and expose a no-ID delivery listener per D-04/D-05."
    - "Success means accepted responsibility, not handled content or a window lifecycle outcome."
    - "Discovery exposes one queryless contract with optional event kinds per manifest tag."
  artifacts:
    - path: packages/core/src/types/intent.ts
      provides: "Canonical intent options, request, contract, result, and delivery types"
    - path: packages/core/src/types/global/service-api.ts
      provides: "URI-shaped IntentApi with onDelivery"
  key_links:
    - from: packages/core/src/types/global/service-api.ts
      to: packages/core/src/types/intent.ts
      via: "IntentInvokeOptions, IntentResult, and IntentDelivery imports"
      pattern: "Intent(InvokeOptions|Delivery|Result)"
---

<objective>
Define the adopted NAP-INTENT public contract at the core source of truth.

Purpose: Give every runtime, SDK, conformance, and documentation plan one unambiguous contract.
Output: Strict public types, exports, global API signatures, JSDoc, and type-level regression coverage.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-PR89-91-DELTA.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Define the public intent request, discovery, acceptance, and delivery types</name>
  <reversibility rating="costly">D-04 changes published TypeScript declarations and requires coordinated downstream updates.</reversibility>
  <files>packages/core/src/types/intent.ts, packages/core/src/index.ts, packages/core/src/intent-contract.test.ts</files>
  <behavior>
    - IntentRequest requires archetype, action, and queryless convention.
    - IntentContract exposes convention plus optional number-array eventKinds; candidates require contracts.
    - IntentResult is a strict accepted/rejected union and IntentDelivery has sender/archetype/action/convention/payload with no ID.
    - Compile failures cover handled, windowId, behavior.newWindow, missing accepted fields, and delivery IDs.
  </behavior>
  <action>Implement D-04/D-07 exactly: add `IntentInvokeOptions`, required normalized `IntentRequest` identity, `IntentContract`, required `IntentCandidate.contracts`, accepted/rejected result branches, and `IntentDelivery`; retain convenience `actions`/`conventions` discovery lists. Remove result handling/window fields and the lifecycle hint not present in the adopted head. Re-export every adopted type from core. Keep payload `unknown`, sender runtime-owned, handler addressing subject to runtime authorization, and event kinds unsigned metadata rather than payload inference.</action>
  <verify><automated>pnpm --filter @napplet/core test:unit -- intent-contract.test.ts &amp;&amp; pnpm --filter @napplet/core type-check</automated></verify>
  <done>Core declarations express every adopted field and reject every retired lifecycle/ID field at compile time.</done>
</task>

<task type="auto">
  <name>Task 2: Replace object-form global intent calls with URI calls and onDelivery</name>
  <files>packages/core/src/types/global/service-api.ts, packages/core/src/types/global.ts</files>
  <action>Per D-03 through D-06, change `IntentApi.invoke` and `open` to `(uri: string, options?: IntentInvokeOptions)`, require `open` callers to supply an `open` convention URI, and add `onDelivery(handler): Subscription`. Rewrite examples/JSDoc around runtime acceptance followed by target-only delivery. State that lifecycle/retry/persistence are runtime policy and sender is endpoint-attested provenance. Do not claim delivery uses INC, expose queue cardinality, or let options override URI-derived identity.</action>
  <verify><automated>pnpm --filter @napplet/core build &amp;&amp; pnpm --filter @napplet/core type-check</automated></verify>
  <done>The browser-global source contract matches the adopted URI and delivery surface with no handling/window assumptions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| napplet API → runtime resolver | Untrusted URI/options request handler resolution and lifecycle effects. |
| runtime → target delivery | Attested provenance and untrusted opaque payload cross to the resolved target. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-45 | Spoofing | IntentDelivery.sender | high | mitigate | Public options/request contain no sender input and delivery types label sender runtime-attested. |
| T-161-46 | Elevation of Privilege | handler selection | high | mitigate | Preserve runtime authorization wording and expose no client-side resolution shortcut. |
</threat_model>

<verification>
Run core unit/type/build gates and inspect generated declarations.
</verification>

<success_criteria>
Core is the authoritative adopted intent contract for all downstream plans.
</success_criteria>

<output>
Create `.planning/phases/161-ad-hoc-convention-package-contracts/161-16-SUMMARY.md` when done.
</output>

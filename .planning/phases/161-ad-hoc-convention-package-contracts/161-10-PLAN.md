---
phase: 161-ad-hoc-convention-package-contracts
plan: "10"
type: execute
wave: 4
depends_on: ["161-01", "161-02", "161-03", "161-04", "161-05", "161-06", "161-07", "161-08", "161-09", "161-11", "161-12"]
files_modified:
  - .changeset/ad-hoc-convention-contracts.md
  - .changeset/ad-hoc-convention-guidance.md
autonomous: true
requirements: [CONV-PKG-06]
must_haves:
  truths:
    - "Every publishable package changed by the phase has release metadata at the correct pre-1.0 bump level."
    - "All focused, repository-wide, conformance, docs/link, AI-slop, active-surface, and diff gates pass."
    - "Only explicit Phase 161 paths are staged; packages/cli/dist-bin and workshop remain untouched."
  artifacts:
    - path: .changeset/ad-hoc-convention-contracts.md
      provides: "Breaking contract package release entries"
    - path: .changeset/ad-hoc-convention-guidance.md
      provides: "Changed shipped docs/skills package release entries"
  key_links:
    - from: git diff
      to: .changeset/ad-hoc-convention-contracts.md
      via: "changed publishable package inventory"
      pattern: "@napplet/"
    - from: package.json
      to: scripts/test-convention-contracts.mjs
      via: "final active-surface gate"
      pattern: "test:convention-contracts"
  prohibitions:
    - statement: "Historical changelogs and archived planning must not be rewritten to erase released meaning."
      status: unresolved
      verification: judgment
    - statement: "Unrelated protocol fields and Nostr event kinds must not be removed by the convention migration."
      status: unresolved
      verification: judgment
---

<objective>
Prepare release metadata, execute the complete verification chain, and make the branch PR-ready.

Purpose: Prove the coordinated clean break is shippable without absorbing unrelated workspace state.
Output: Complete changesets, green release evidence, explicit staging safety, and PR readiness.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-RESEARCH.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-VALIDATION.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-01-SUMMARY.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-02-SUMMARY.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-03-SUMMARY.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-04-SUMMARY.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-05-SUMMARY.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-06-SUMMARY.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-07-SUMMARY.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-08-SUMMARY.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-09-SUMMARY.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-11-SUMMARY.md
@.planning/phases/161-ad-hoc-convention-package-contracts/161-12-SUMMARY.md
</context>

## Artifacts this phase produces

- Breaking-contract and guidance changesets
- Full repository/release verification evidence in `161-10-SUMMARY.md`
- A pushed, PR-ready Phase 161 branch

<tasks>

<task type="tracer">
  <name>Task 1: Add complete package release metadata</name>
  <read_first>
    - AGENTS.md
    - .changeset/config.json
    - package.json
    - packages/core/package.json
    - packages/nap/package.json
    - packages/vite-plugin/package.json
    - packages/cli/package.json
    - packages/conformance/package.json
    - packages/conformance-cli/package.json
    - packages/sdk/package.json
    - packages/shim/package.json
    - packages/boilerplate/package.json
    - packages/skills/package.json
  </read_first>
  <files>.changeset/ad-hoc-convention-contracts.md, .changeset/ad-hoc-convention-guidance.md</files>
  <action>Derive the final changed publishable package set from the explicit Phase 161 diff. Put breaking public contract changes for core, nap, Vite plugin, CLI, and any conformance public response surface in the contract changeset using minor bumps under the repository's 0.x policy. Put shipped documentation/skill-only changes for conformance-cli, SDK, shim, boilerplate, and skills in the guidance changeset using patch bumps unless their actual diff changes public code, in which case use minor. Omit apps and root-only scripts. Every changed publishable package must appear exactly once across the two files.</action>
  <acceptance_criteria>
    - Changed publishable packages and changeset entries are a bijection.
    - Breaking 0.x contract packages receive minor bumps.
    - Apps, tests-only, and root scripts receive no package bump.
  </acceptance_criteria>
  <verify><automated>pnpm changeset status &amp;&amp; pnpm check:jsr</automated></verify>
  <done>Release metadata covers every shipped package change at the correct bump level.</done>
</task>

<task type="auto">
  <name>Task 2: Run the complete Phase 161 release gate</name>
  <read_first>
    - package.json
    - .aislop/config.yml
    - scripts/test-convention-contracts.mjs
    - .planning/phases/161-ad-hoc-convention-package-contracts/161-VALIDATION.md
  </read_first>
  <files>None (verification-only)</files>
  <action>Run every automated gate and record command/result evidence in the summary: focused package tests from Plans 01-07; `test:convention-contracts`; full build, type-check, recursive unit tests, aggregate test, conformance, tutorials, docs build, link check, lint, JSR check, AI-slop changed-surface scan, changeset status, and diff check. Re-run the active guard after all docs/skills changes. Any failure blocks completion; do not disable a rule, relax an allowlist, or resolve web#183 to make a gate pass.</action>
  <acceptance_criteria>
    - Every CONV-PKG-01..06 gate is green and recorded.
    - Active-surface guard is green after migration.
    - AI-slop uses the pinned repository configuration with no new suppression.
    - web#183 ambiguities remain non-goals.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:convention-contracts &amp;&amp; pnpm build &amp;&amp; pnpm type-check &amp;&amp; pnpm -r test:unit &amp;&amp; pnpm test &amp;&amp; pnpm test:conformance &amp;&amp; pnpm test:tutorial &amp;&amp; pnpm --filter @napplet/docs build &amp;&amp; pnpm check:links &amp;&amp; pnpm lint &amp;&amp; pnpm check:jsr &amp;&amp; pnpm dlx aislop@0.12.0 scan --changes --base origin/main . &amp;&amp; pnpm changeset status &amp;&amp; git diff --check</automated>
  </verify>
  <done>The full repository and release verification chain passes without exceptions.</done>
</task>

<task type="auto">
  <name>Task 3: Enforce explicit staging safety and prepare the PR</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/161-ad-hoc-convention-package-contracts/161-CONTEXT.md
    - .planning/phases/161-ad-hoc-convention-package-contracts/161-10-SUMMARY.md
  </read_first>
  <files>None (git and PR metadata only)</files>
  <action>Inspect the final diff against `origin/main`, confirm only the Phase 161 inventory plus planning/changeset/guard files changed, and confirm no committed path is under `packages/cli/dist-bin` or `workshop`. Stage only explicit paths; never use broad add flags. Push the existing feature branch and prepare/open the PR through the repository shipping flow with pinned upstream commit, issue web#183 non-goals, changed packages, changesets, full verification, and untouched-workspace notes. If the execution orchestrator reserves PR creation for `$gsd-ship`, leave a complete PR body/evidence handoff and report the exact next command.</action>
  <acceptance_criteria>
    - No unrelated or excluded path is staged or committed.
    - PR metadata cites upstream commit 6461e4b37c29dc09a20dff35d9515889c4433874 and all verification.
    - The branch is ready for the configured shipping workflow.
  </acceptance_criteria>
  <verify><automated>test -z "$(git diff --name-only origin/main...HEAD -- packages/cli/dist-bin workshop)" &amp;&amp; git diff --check &amp;&amp; git status --short</automated></verify>
  <done>The verified change is isolated, pushed or handed to `$gsd-ship`, and PR-ready.</done>
</task>

</tasks>

<threat_model>
ASVS level: 1. Blocking threshold: high.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository diff → package release | Source and docs changes become published package versions. |
| shared workspace → git/PR | Unrelated untracked state could be accidentally staged or disclosed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-161-28 | Tampering | release package set | high | mitigate | Diff-to-changeset bijection and changeset status gate every changed publishable package. |
| T-161-29 | Information Disclosure | shared workspace staging | high | mitigate | Explicit-path staging and committed-path checks exclude dist-bin/workshop. |
| T-161-30 | Spoofing | PR verification claims | medium | mitigate | PR body is generated from recorded command evidence and pinned upstream provenance. |
</threat_model>

<verification>
All repository gates pass; changesets cover shipped packages; excluded workspace paths are absent from committed diff.
</verification>

<success_criteria>
Phase 161 is release-complete, isolated from unrelated work, and ready for review/shipping.
</success_criteria>

<output>
Create `.planning/phases/161-ad-hoc-convention-package-contracts/161-10-SUMMARY.md` when done.
</output>

---
phase: "159.1"
plan: "159.1-02"
status: complete
completed: 2026-07-21
commits: [a6f9f0a, 842a2b3]
requirements: [ONB-01, ONB-02, ONB-05, ONB-08]
---

# Scaffold and standalone binary delivery

The boilerplate now accepts only project/scaffold inputs, derives the package
name from the target, and leaves template deployment metadata untouched. The
JSR release workflow compiles all five supported targets, publishes a checksum
manifest to versioned and rolling stable releases, and feeds checksum-verifying
Unix and PowerShell installers.

Verification:
- `pnpm --filter @napplet/boilerplate build type-check test:unit` (6 passed)
- `pnpm test:installers` including tamper rejection
- `pnpm test:workflow-contracts` (3 passed)
- Real `deno compile` output confirmed at `packages/cli/dist`.

---
phase: "159.1"
plan: "159.1-04"
status: complete
completed: 2026-07-21
commits: [6c59726, 323cdbc, a81191d, fd2213b]
requirements: [ONB-01, ONB-02, ONB-03, ONB-04, ONB-05, ONB-06, ONB-07, ONB-08]
---

# Visible onboarding and release verification

The SPA now presents the complete install -> create -> init -> skills -> build
-> dry-run -> deploy path, with a macOS/Linux/Windows installer selector and a
copy control for every command. Responsive browser checks cover desktop,
tablet, mobile, and 320px-wide layouts with overflow and control-stability
assertions.

Release metadata covers `@napplet/cli`, `@napplet/boilerplate`, and
`@napplet/skills`. Final review also moved manifest metadata handling into a
focused module and corrected every installer surface to the canonical
`napplet/web` repository URL.

Verification:
- Full monorepo build, type-check, unit, and tutorial suites passed.
- Installer checksum/tamper, release workflow, and onboarding contracts passed.
- SPA production build and four Playwright viewport checks passed.
- Assembled SPA/docs link crawl checked 23 URLs with no broken links.
- Changed-code quality engines are clean; the score is 95/100 only because of
  the existing transitive `js-yaml` advisory from Changesets.
- `git diff --check` is clean.

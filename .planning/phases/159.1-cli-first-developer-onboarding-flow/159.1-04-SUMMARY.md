---
phase: "159.1"
plan: "159.1-04"
status: complete
completed: 2026-07-21
reviewed: 2026-07-22
commits: [6c59726, 323cdbc, a81191d, fd2213b, 3edae5f, 4aa5d9e]
requirements: [ONB-01, ONB-02, ONB-03, ONB-04, ONB-05, ONB-06, ONB-07, ONB-08]
---

# Visible onboarding and release verification

Maintainer review replaced the rejected six-step SPA with the original compact
panel shape. The alpha warning, blur, inert state, and acknowledgement are
restored; the unlocked panel contains one copyable command:
`curl -fsSL https://napplet.run/install.sh | sh`.

The web build now emits checksum-verifying installers at `/install.sh` and
`/install.ps1`. Successful installers end with bold colored guidance to try
`'napplet guide'`. That command owns the remaining create, init, skills, build,
dry-run, and deploy sequence, with a relevant docs URL at each stage and a final
documentation block.

Root, package, and VitePress onboarding docs use the hosted installer URLs and
make `napplet guide` discoverable. Regression tests enforce source/public/built
installer parity, hosted URL usage, guide ordering and links, the restored alpha
gate, and the absence of the six workflow rows from the SPA.

Verification:

- Full monorepo build, type-check, unit, aggregate test, and tutorial suites passed.
- CLI unit coverage passes 111 tests, including the linked guide output.
- Installer checksum/tamper, release workflow, and five onboarding contracts passed.
- SPA checks passed warning and accepted states at four responsive viewports.
- Eight screenshots were inspected with no content overlap or horizontal overflow.
- Assembled SPA/docs link crawl checked 23 URLs with no broken links; hosted
  installer responses matched their tested sources byte-for-byte.
- Changed-code formatting, quality, AI-slop, and lint engines have zero findings;
  the 95/100 score reflects only the existing transitive `js-yaml` advisory.
- `git diff --check` and stale raw-installer URL scans are clean.

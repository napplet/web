---
phase: "159.1"
plan: "159.1-04"
type: implementation
wave: 3
depends_on: ["159.1-01", "159.1-02", "159.1-03"]
files_modified:
  - apps/web/src/lib/GetStarted.svelte
  - apps/web/src/lib/GetStarted.test.ts
  - .changeset
  - .planning/phases/159.1-cli-first-developer-onboarding-flow/159.1-VERIFICATION.md
requirements: [ONB-01, ONB-02, ONB-03, ONB-04, ONB-05, ONB-06, ONB-07, ONB-08]
autonomous: true
must_haves:
  truths:
    - "SPA visibly shows install, create, init, skills, build, and deploy without an alpha gate"
    - "Platform-specific install commands and every workflow command are copyable"
    - "The layout has no overlap or horizontal overflow at required desktop and mobile viewports"
    - "All package, docs, workflow, and repository gates pass before push"
---

> **Maintainer review revision (2026-07-22):** The first task's unblocked
> six-step SPA was rejected after execution. The accepted surface restores the
> alpha warning and original one-command shape, hosts the installer at
> `napplet.run/install.sh`, and moves the remaining workflow to `napplet guide`.
> The current contract and evidence are in `159.1-UI-SPEC.md` and
> `159.1-VERIFICATION.md`; the task below is retained as execution history.

<objective>
Ship the visible onboarding experience, package release metadata, and recorded
end-to-end verification for the complete goal.
</objective>

<execution_context>
@AGENTS.md
@.planning/phases/159.1-cli-first-developer-onboarding-flow/159.1-UI-SPEC.md
@.planning/phases/159.1-cli-first-developer-onboarding-flow/159.1-VALIDATION.md
</execution_context>

<threat_model>
- Render commands as inert text and copy them through the Clipboard API only.
- Keep external links explicit and preserve safe `rel` attributes.
- Do not expose secrets or perform a live deploy during visual or smoke testing.
</threat_model>

<tasks>
<task type="auto" tdd="true">
  <name>Build the responsive SPA workflow</name>
  <files>apps/web/src/lib/GetStarted.svelte, apps/web/src/lib/GetStarted.test.ts</files>
  <action>Replace the blurred alpha modal with the UI-spec six-step surface, a macOS/Linux/Windows segmented installer selector, stable copy controls, real commands, accessible labels/focus, and deterministic component tests.</action>
  <verify>Web unit/build tests pass and Playwright screenshots at 1440x900, 1024x768, 390x844, and 320x568 show no overlap or overflow.</verify>
  <done>The first viewport visibly changes and users can follow/copy the complete path on mobile and desktop.</done>
</task>
<task type="auto">
  <name>Add release changesets and run final quality gates</name>
  <files>.changeset, .planning/phases/159.1-cli-first-developer-onboarding-flow/159.1-VERIFICATION.md</files>
  <action>Add scoped changesets for CLI, boilerplate, and skills where shipped content changed. Run build, type, unit, docs, tutorial, workflow, slop, diff, installer smoke, and visual checks; record exact commands/results and fix failures in scope.</action>
  <verify>Every command in VALIDATION.md exits zero and `git diff --check` is clean.</verify>
  <done>Verification evidence is committed and the branch is ready to push without waivers.</done>
</task>
</tasks>

<verification>
- `pnpm build`
- `pnpm type-check`
- `pnpm -r test:unit`
- `pnpm docs:build`
- `pnpm tutorials:validate`
- `pnpm test:workflow-contracts`
- `pnpm lint:slop`
- `git diff --check`
- Playwright responsive screenshots and DOM overflow assertions
</verification>

<success_criteria>
- The SPA is visibly different and matches the canonical CLI-first funnel.
- All ONB requirements have automated or recorded verification evidence.
- The branch can be pushed and reviewed as a non-empty PR.
</success_criteria>

<output>159.1-04-SUMMARY.md and 159.1-VERIFICATION.md</output>

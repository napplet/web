---
phase: "159.1"
plan: "159.1-01"
status: complete
completed: 2026-07-21
commits: [6e05ad4]
requirements: [ONB-02, ONB-03, ONB-04, ONB-05, ONB-08]
---

# CLI composition and metadata ownership

`napplet create` and `napplet skills` now delegate to the maintained npm
packages through explicit argv arrays. `napplet init` persists a validated
named d-tag, title, optional description, and canonical NAAT contracts; deploy
manifest generation gives that config precedence over HTML/plugin defaults and
preserves old configs as a fallback.

Verification:
- `deno task --config packages/cli/deno.json check`
- `deno task --config packages/cli/deno.json test:unit` (110 passed)
- Compiled and executed `napplet-darwin-aarch64 --help` successfully.

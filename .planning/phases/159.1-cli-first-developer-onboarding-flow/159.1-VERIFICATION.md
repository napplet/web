---
phase: "159.1"
verified: 2026-07-22T00:19:07Z
status: passed
score: 8/8 requirements satisfied
re_verification: true
---

# Phase 159.1 Verification Report

**Phase Goal:** Deliver one coherent developer path from a hosted standalone CLI
installer through scaffold creation, deployment metadata, agent skills, build
checks, and deployment. Keep the SPA concise and put the executable workflow and
documentation links in `napplet guide` and the docs.

**Status:** passed

## Goal Achievement

| Truth | Status | Evidence |
|-------|--------|----------|
| Standalone binaries install without Deno | VERIFIED | Five Deno compile targets, checksum release workflow, shell/PowerShell installers, and checksum-tamper preservation pass. |
| Installers have stable site routes | VERIFIED | Vite emits `/install.sh` and `/install.ps1`; assembled-site responses are byte-identical to the tested source scripts. |
| CLI composes and explains the complete path | VERIFIED | `napplet create`, `init`, `skills`, deploy commands, and `napplet guide` are implemented and covered by 111 passing CLI tests. |
| CLI owns deployment metadata | VERIFIED | `.napplet/config.json` stores name/title/description/archetypes; manifest tests prove config precedence and canonical tags. |
| Boilerplate only scaffolds project files | VERIFIED | Generator prompts/options no longer collect deployment metadata; six generator/guidance tests pass. |
| Guidance uses one ordered workflow | VERIFIED | Five onboarding contracts cover root docs, package docs, VitePress, shipped skills, hosted installers, and the compact SPA. |
| SPA is concise without hiding the alpha warning | VERIFIED | The initial gate blurs and locks the panel; acknowledgement reveals one hosted installer command and `napplet guide`. |
| Repository gates pass | VERIFIED | Build, type, unit, aggregate test/tutorial, docs, link, workflow, installer, browser, quality, lint, stale-string, and diff checks pass. |

## Requirements Coverage

| Requirement | Status | Evidence |
|-------------|--------|----------|
| ONB-01 | SATISFIED | Linux x86_64/aarch64, macOS x86_64/aarch64, and Windows x86_64 binaries compile with SHA-256 verification; Deno install remains documented. |
| ONB-02 | SATISFIED | `napplet create` safely passes argv to `@napplet/boilerplate`; generator metadata prompts and mutations are removed. |
| ONB-03 | SATISFIED | Init/config/manifest tests cover d-tag, title, description, canonical archetype contracts, precedence, and legacy config behavior. |
| ONB-04 | SATISFIED | `napplet skills` safely delegates to `@napplet/skills`, including Windows runner and target passthrough tests. |
| ONB-05 | SATISFIED | CLI help, installer completion, generator completion, wizard output, and docs route developers to the next real command while preserving automation. |
| ONB-06 | SATISFIED | SPA restores the alpha gate and one hosted installer command; `napplet guide` carries the ordered remaining stages and docs links; four viewports have no measured overlap or overflow. |
| ONB-07 | SATISFIED | Root/package/VitePress/tutorial/skill surfaces share the CLI-first workflow; package bypass, raw installer URLs, and Deno-only primary funnels are absent. |
| ONB-08 | SATISFIED | Behavioral, installer, workflow, docs, browser, build, type, unit, quality, lint, and diff checks pass; release changeset is present. |

## Verification Commands

| Command | Result |
|---------|--------|
| `pnpm build` | PASS: 13/13 build tasks, including five standalone CLI binaries and SPA/docs output. |
| `pnpm type-check` | PASS: 17/17 tasks; Svelte reports 0 errors and 0 warnings. |
| `pnpm -r test:unit` | PASS: all workspace suites, including CLI 111, boilerplate 6, skills 14, core 33, nap 119, and conformance packages. |
| `pnpm test` | PASS: JSR exports, 23 unit tasks, tutorial package checks, generated tutorial build, and conformance (5 pass, 5 expected skips). |
| `pnpm test:installers` | PASS: install, checksum rejection, final guide prompt, ANSI styling, and source/public parity. |
| `pnpm test:workflow-contracts` | PASS: 3/3 binary release workflow contracts. |
| `pnpm test:onboarding-contracts` | PASS: 5/5 cross-surface workflow and hosted URL contracts. |
| `pnpm test:spa-onboarding` | PASS: warning and accepted states at 1440x900, 1024x768, 390x844, and 320x568. |
| Assembled-site `scripts/check-links.mjs` | PASS: 23 URLs, 0 broken; `/install.sh` and `/install.ps1` match source. |
| `pnpm lint` | PASS: repository has no configured lint tasks. |
| `npx --yes aislop@0.12.0 scan --changes --base origin/main` | PASS at configured gate: formatting, code quality, AI-slop, and lint have 0 findings; 95/100 from the pre-existing Changesets `js-yaml` advisory. |
| `git diff --check` and stale URL scan | PASS. |

## Visual Evidence

- `/tmp/napplet-onboarding-desktop-warning.png` and `/tmp/napplet-onboarding-desktop.png` (1440x900)
- `/tmp/napplet-onboarding-tablet-warning.png` and `/tmp/napplet-onboarding-tablet.png` (1024x768)
- `/tmp/napplet-onboarding-mobile-warning.png` and `/tmp/napplet-onboarding-mobile.png` (390x844)
- `/tmp/napplet-onboarding-narrow-warning.png` and `/tmp/napplet-onboarding-narrow.png` (320x568)

All screenshots were inspected. The warning remains prominent, the unlocked
surface preserves the original one-command composition, the hosted URL wraps
inside its control, links remain readable, and the document has no horizontal
overflow. The narrow screenshot harness hides the fixed header only during
element capture after separately asserting that the real responsive menu is
closed.

## Residual Risk

`https://napplet.run/install.sh` and `/install.ps1` become live only after this PR
merges and the site deployment completes. Their assembled production artifacts
were served and compared locally. The quality scanner also reports the existing
transitive `js-yaml` advisory through `@changesets/cli`; all in-repository quality
engines are clean.

## Gaps Summary

No in-scope implementation gaps. Production URL verification remains a
post-merge deployment check.

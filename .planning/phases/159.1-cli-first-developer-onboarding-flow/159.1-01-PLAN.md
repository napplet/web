---
phase: "159.1"
plan: "159.1-01"
type: implementation
wave: 1
depends_on: []
files_modified:
  - packages/cli/src/main.ts
  - packages/cli/src/config.ts
  - packages/cli/src/init.ts
  - packages/cli/src/init-wizard.ts
  - packages/cli/src/manifest.ts
  - packages/cli/src/main.test.ts
  - packages/cli/src/config.test.ts
  - packages/cli/src/init.test.ts
  - packages/cli/src/manifest.test.ts
requirements: [ONB-02, ONB-03, ONB-04, ONB-05, ONB-08]
autonomous: true
must_haves:
  truths:
    - "napplet create and napplet skills invoke shipped package CLIs with explicit argv arrays"
    - "napplet init owns named d-tag, title, description, and canonical NAAT archetype contracts"
    - "Config metadata overrides template metadata without emitting a generic type tag"
    - "Existing configs and non-interactive flags remain valid"
  artifacts:
    - packages/cli/src/config.ts
    - packages/cli/src/manifest.ts
    - packages/cli/src/main.test.ts
---

<objective>
Make the primary CLI the owner and coordinator of project creation, deployment
metadata, skill installation, and next-step guidance.
</objective>

<execution_context>
@AGENTS.md
@.planning/phases/159.1-cli-first-developer-onboarding-flow/159.1-PRD.md
@.planning/phases/159.1-cli-first-developer-onboarding-flow/159.1-RESEARCH.md
</execution_context>

<threat_model>
- Treat target paths and passthrough arguments as argv, never shell source.
- Validate named d-tags with the NIP-5A grammar before persisting or deploying.
- Emit only canonical `archetype` tags; never infer or invent protocol identifiers.
- Do not expose key material in new completion output or command errors.
</threat_model>

<tasks>
<task type="auto" tdd="true">
  <name>Add safe create and skills composition commands</name>
  <files>packages/cli/src/main.ts, packages/cli/src/main.test.ts</files>
  <action>Add `napplet create` and `napplet skills` dispatch using the existing process runner with explicit argument arrays, stable help, actionable missing-Node errors, and tests that inject a runner instead of accessing the network.</action>
  <verify>`deno test -A packages/cli/src/main.test.ts` passes.</verify>
  <done>Both commands preserve arguments exactly and route users to the next workflow command.</done>
</task>
<task type="auto" tdd="true">
  <name>Persist deploy metadata in CLI configuration</name>
  <files>packages/cli/src/config.ts, packages/cli/src/init.ts, packages/cli/src/init-wizard.ts, packages/cli/src/config.test.ts, packages/cli/src/init.test.ts</files>
  <action>Add backwards-compatible optional metadata for name, title, description, and canonical `slug:protocol` archetype contracts. Extend prompts and automation flags, validate inputs, preserve old configs, and print exact skills/build/deploy next steps.</action>
  <verify>`deno test -A packages/cli/src/config.test.ts packages/cli/src/init.test.ts` passes.</verify>
  <done>Interactive and non-interactive init write the same valid metadata contract.</done>
</task>
<task type="auto" tdd="true">
  <name>Apply config metadata precedence to deploy manifests</name>
  <files>packages/cli/src/manifest.ts, packages/cli/src/manifest.test.ts</files>
  <action>Merge valid config title/description/archetypes over HTML and plugin defaults, preserve existing fallback behavior, copy canonical archetype tags into manifest snapshots, and prove no generic type tag appears.</action>
  <verify>`deno test -A packages/cli/src/manifest.test.ts` passes.</verify>
  <done>Manifest output reflects config metadata deterministically and remains NIP-5A/NAAT compliant.</done>
</task>
</tasks>

<verification>
- `deno task --config packages/cli/deno.json test`
- `deno task --config packages/cli/deno.json check`
</verification>

<success_criteria>
- CLI command tests cover argv safety, metadata validation, compatibility, and precedence.
- `napplet --help` exposes the ordered onboarding commands.
</success_criteria>

<output>159.1-01-SUMMARY.md</output>

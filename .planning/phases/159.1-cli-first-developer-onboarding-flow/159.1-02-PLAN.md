---
phase: "159.1"
plan: "159.1-02"
type: implementation
wave: 1
depends_on: []
files_modified:
  - packages/boilerplate/src/index.js
  - packages/boilerplate/src/index.test.js
  - packages/boilerplate/README.md
  - scripts/install-napplet-cli.sh
  - scripts/install-napplet-cli.ps1
  - scripts/test-install-napplet-cli.mjs
  - .github/workflows/publish-jsr.yml
requirements: [ONB-01, ONB-02, ONB-05, ONB-08]
autonomous: true
must_haves:
  truths:
    - "The boilerplate asks only for a project location and never owns deployment metadata"
    - "Release binaries cover the five existing Deno compile targets and include SHA256SUMS"
    - "Installers verify checksums before replacing an executable"
  artifacts:
    - packages/boilerplate/src/index.js
    - scripts/install-napplet-cli.sh
    - scripts/install-napplet-cli.ps1
    - .github/workflows/publish-jsr.yml
---

<objective>
Reduce scaffolding to project creation and publish checksum-verified standalone
CLI binaries so onboarding does not require Deno.
</objective>

<execution_context>
@AGENTS.md
@.planning/phases/159.1-cli-first-developer-onboarding-flow/159.1-RESEARCH.md
@.planning/phases/159.1-cli-first-developer-onboarding-flow/159.1-VALIDATION.md
</execution_context>

<threat_model>
- Never evaluate target paths, archive names, release metadata, or installer input as shell code.
- Verify an asset checksum before moving it into the install directory.
- Fail closed for unsupported platforms, missing checksum entries, and failed downloads.
- Grant `contents: write` only to the release job that uploads versioned assets.
</threat_model>

<tasks>
<task type="auto" tdd="true">
  <name>Simplify the maintained boilerplate generator</name>
  <files>packages/boilerplate/src/index.js, packages/boilerplate/src/index.test.js, packages/boilerplate/README.md</files>
  <action>Remove title/name/type prompts and mutations, derive package name from the target directory, retain template and force automation, and end with `napplet init` and `napplet skills` guidance. Add subprocess and filesystem tests.</action>
  <verify>`pnpm --filter @napplet/boilerplate test:unit` passes.</verify>
  <done>The generator only creates a project and cannot silently define deploy metadata.</done>
</task>
<task type="auto" tdd="true">
  <name>Add portable checksum-verifying installers</name>
  <files>scripts/install-napplet-cli.sh, scripts/install-napplet-cli.ps1, scripts/test-install-napplet-cli.mjs</files>
  <action>Map supported OS/architecture pairs to stable release asset names, download the executable and SHA256SUMS, verify before atomic installation, support install-dir/release-base overrides for smoke tests, and fail clearly on unsupported systems.</action>
  <verify>`node scripts/test-install-napplet-cli.mjs` and `bash -n scripts/install-napplet-cli.sh` pass; PowerShell syntax is checked when `pwsh` is available.</verify>
  <done>Installers cannot install a tampered or unlisted asset.</done>
</task>
<task type="auto">
  <name>Compile and publish standalone release assets</name>
  <files>.github/workflows/publish-jsr.yml</files>
  <action>Add a least-privilege matrix over the five existing `deno compile` tasks, upload immutable artifacts, aggregate checksums, and publish both a versioned release and the stable installer release without changing JSR publication.</action>
  <verify>Workflow YAML parses, every compile target and installer asset is statically covered, and action versions are pinned consistently with repository policy.</verify>
  <done>Each CLI release produces five binaries plus SHA256SUMS consumable by both installers.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @napplet/boilerplate test:unit`
- `node scripts/test-install-napplet-cli.mjs`
- `pnpm test:workflow-contracts`
</verification>

<success_criteria>
- Scaffolding has no deployment metadata prompt.
- Standalone release/install contract is deterministic, checksum-verified, and statically tested.
</success_criteria>

<output>159.1-02-SUMMARY.md</output>

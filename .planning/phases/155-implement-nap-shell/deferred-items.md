# Deferred Items — Phase 155

## Out-of-scope doc residue (Phase 154 deferral cleanup miss)

- `apps/docs/packages/shim.md` lines ~44-45 still list `connect` and `class` rows
  in the runtime-API table. Those domains were deferred/removed in Phase 154
  (DEFER-01/DEFER-02). Not touched here — outside Phase 155's NAP-SHELL scope and
  unrelated to the files this plan owns. Should be swept in a follow-up docs pass.

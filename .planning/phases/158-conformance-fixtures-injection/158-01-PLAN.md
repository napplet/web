---
phase: 158
plan: "158-01"
type: implementation
wave: 5
depends_on:
  - "157-03"
files_modified:
  - packages/conformance/src/run/boot.ts
  - packages/conformance/src/shell/reference-shell.ts
  - packages/conformance/src/validators/envelope.ts
  - packages/conformance/src/checks/catalog.ts
requirements:
  - NIP5D-01
  - NIP5D-02
  - CONF-01
  - CONF-02
  - CONF-04
autonomous: true
must_haves:
  truths:
    - Conformance must validate current NIP-5D/NAP behavior, not private shim handshakes.
    - Runtime injection happens before fixture code and outside signed artifact bytes.
    - Conformance hard errors require exact NIP-5D or NAP citations.
  artifacts:
    - packages/conformance/src/run/boot.ts
    - packages/conformance/src/validators/envelope.ts
  key_links:
    - https://github.com/nostr-protocol/nips/pull/2303
    - https://github.com/napplet/naps
---

<objective>
Move conformance boot/runtime behavior from shell handshake detection to injected
domain namespace collection.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Inject runtime namespace before fixture scripts</name>
  <files>packages/conformance/src/run/boot.ts, packages/conformance/src/shell/reference-shell.ts</files>
  <action>Change boot harness/reference runtime to inject `window.napplet` domains before subject code instead of waiting for `shell.ready`.</action>
  <verify>`pnpm --filter @napplet/conformance test:unit -- boot reference-shell` passes.</verify>
  <done>Boot checks observe injected namespace without handshake messages.</done>
</task>

<task type="auto">
  <name>Remove shell envelope validation</name>
  <files>packages/conformance/src/validators/envelope.ts, packages/conformance/src/checks/catalog.ts</files>
  <action>Remove shell.ready/init envelope specs, foundational-domain exemptions, and check titles that imply generic shell support.</action>
  <verify>`rg "shell\\.ready|shell\\.init|window\\.napplet\\.shell|shell\\.supports|NAP-SHELL" packages/conformance/src` has zero live-code matches.</verify>
  <done>Conformance validates NAP domain envelopes only.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @napplet/conformance test:unit`
- Focused stale-surface grep over `packages/conformance/src`.
</verification>

<success_criteria>
- Conformance no longer requires shell handshake traffic.
- Normative checks cite current NIP-5D/NAP requirements.
</success_criteria>

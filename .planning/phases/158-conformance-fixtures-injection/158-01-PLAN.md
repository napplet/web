---
phase: 158
plan: "158-01"
type: implementation
wave: 2
depends_on:
  - "157-01"
files_modified:
  - packages/conformance/src
  - packages/conformance-cli/src
  - apps/conformance/src
  - tests/fixtures/napplets
requirements:
  - NIP5D-01
  - NIP5D-02
  - CONF-01
  - CONF-02
  - CONF-03
  - CONF-04
  - CONF-05
autonomous: true
---

<objective>
Make conformance validate the current NIP-5D bootstrap boundary: runtime-injected
`window.napplet` before subject code and valid NAP domain envelopes only.
</objective>

<tasks>
1. Change boot collection to inject the shim/runtime namespace before fixture
   code executes instead of waiting for `shell.ready`.
2. Remove `shell.ready` / `shell.init` envelope specs, foundational-domain drift
   exemptions, and reference-shell handshake responses.
3. Replace generic `supports() = false` degradation checks with domain absence
   checks.
4. Update conformant/broken fixtures to exercise domain traffic without shell
   handshake traffic.
5. Align CLI and web conformance runtime behavior and tests.
</tasks>

<verification>
- `pnpm --filter @napplet/conformance test:unit`
- `pnpm --filter @napplet/conformance-cli test:unit`
- `pnpm test:conformance`
- Fixture pass/fail assertions for injected runtime namespace.
</verification>

<success_criteria>
- Conformance no longer requires any shell handshake message.
- Fixture success proves injected namespace boot.
- Broken fixture still fails for malformed/invalid domain behavior.
</success_criteria>

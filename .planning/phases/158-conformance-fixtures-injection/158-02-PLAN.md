---
phase: 158
plan: "158-02"
type: implementation
wave: 6
depends_on:
  - "158-01"
files_modified:
  - packages/conformance-cli/src
  - apps/conformance/src
  - tests/fixtures/napplets
requirements:
  - NIP5D-01
  - NIP5D-02
  - CONF-01
  - CONF-03
  - CONF-05
autonomous: true
must_haves:
  truths:
    - Fixtures and CLI/web runners prove injected namespace/domain absence, not generic supports fallback.
    - Broken fixtures still fail for invalid domain behavior.
  artifacts:
    - tests/fixtures/napplets
    - packages/conformance-cli/src
    - apps/conformance/src
  key_links:
    - packages/conformance/src/run/boot.ts -> tests/fixtures/napplets
---

<objective>
Align CLI, web runner, and fixtures with injected domain namespace behavior.
</objective>

<tasks>
<task type="auto" tdd="true">
  <name>Update fixtures for injected domains</name>
  <files>tests/fixtures/napplets</files>
  <action>Rewrite conformant and broken fixtures so they use available domain objects directly and treat absent properties as unsupported domains.</action>
  <verify>`pnpm test:conformance` exercises fixture pass/fail expectations.</verify>
  <done>Fixture outcomes no longer depend on shell.ready, shell.init, or shell.supports.</done>
</task>

<task type="auto">
  <name>Align CLI and web runner tests</name>
  <files>packages/conformance-cli/src, apps/conformance/src</files>
  <action>Update CLI and web conformance runtime behavior/tests to consume injected namespace boot results.</action>
  <verify>`pnpm --filter @napplet/conformance-cli test:unit` and relevant app build/test commands pass.</verify>
  <done>Both runners report current injected-runtime conformance semantics.</done>
</task>
</tasks>

<verification>
- `pnpm --filter @napplet/conformance-cli test:unit`
- `pnpm test:conformance`
- Relevant `apps/conformance` build/test command from package scripts.
</verification>

<success_criteria>
- CLI/web/fixture surfaces agree with injected-domain runtime model.
</success_criteria>

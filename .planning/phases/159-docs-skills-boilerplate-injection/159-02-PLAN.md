---
phase: 159
plan: "159-02"
type: docs
wave: 7
depends_on:
  - "157-04"
  - "158-02"
files_modified:
  - packages/skills
  - skills
  - packages/boilerplate
requirements:
  - NIP5D-01
  - NIP5D-03
  - DOC-03
  - DOC-04
autonomous: true
must_haves:
  truths:
    - Skills and boilerplate must not tell agents/authors to import `@napplet/shim` as napplet-owned bootstrap.
    - Generated guidance should use injected domain property presence and SDK/types only.
  artifacts:
    - packages/skills
    - packages/boilerplate
  key_links:
    - packages/skills -> skills
---

<objective>
Align agent-facing skills and boilerplate guidance with runtime-owned injection.
</objective>

<tasks>
<task type="auto">
  <name>Update napplet skills</name>
  <files>packages/skills, skills</files>
  <action>Rewrite skill guidance so generated napplets consume injected `window.napplet` domains and never add stale shell bootstrap/supports plumbing.</action>
  <verify>`rg "shell\\.supports|shell\\.ready|shell\\.init|window\\.napplet\\.shell|NAP-SHELL|@napplet/nap/shell" packages/skills skills` returns zero live guidance matches or classified historical exceptions.</verify>
  <done>Agents using repo skills build against current NIP-5D semantics.</done>
</task>

<task type="auto">
  <name>Update boilerplate guidance</name>
  <files>packages/boilerplate</files>
  <action>Check generated template docs/scripts and align any stale runtime bootstrap guidance with injected domains.</action>
  <verify>`rg "shell\\.supports|shell\\.ready|shell\\.init|window\\.napplet\\.shell|NAP-SHELL|@napplet/nap/shell" packages/boilerplate` returns zero live guidance matches or classified historical exceptions.</verify>
  <done>Boilerplate does not teach stale shell handshake or napplet-owned injection.</done>
</task>
</tasks>

<verification>
- Stale-surface grep over `packages/skills`, `skills`, and `packages/boilerplate`.
- `git diff --check`
</verification>

<success_criteria>
- Skills and boilerplate produce/describe spec-faithful injected-domain napplets.
</success_criteria>

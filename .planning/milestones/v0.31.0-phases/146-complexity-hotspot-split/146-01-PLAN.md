---
phase: 146
plan: 1
title: "Split low-risk complexity hotspots"
wave: 1
depends_on: [145]
requirements_addressed: [QUAL-01, QUAL-02, QUAL-03, QUAL-04]
files_modified:
  - packages/nub/src/connect/types.ts
  - packages/vite-plugin/src/index.ts
autonomous: true
---

# Plan 146-01: Split Low-Risk Complexity Hotspots

## Objective

Reduce real complexity in low-risk long functions and document exact deferrals for large public files that need API-boundary planning.

## Tasks

<task id="146-01-01">
<title>Split connect origin normalization</title>

<action>
Extract helper functions from `normalizeConnectOrigin` for scheme splitting, rest validation, host/port parsing, IPv4 validation, and DNS label validation.
</action>

<acceptance_criteria>
- `normalizeConnectOrigin` no longer trips the function-length warning.
- Existing connect normalizer and aggregate-hash tests still pass.
</acceptance_criteria>
</task>

<task id="146-01-02">
<title>Split Vite plugin long functions</title>

<action>
Extract the schema walker and plugin hook bodies into smaller local helpers while preserving outputs and ordering.
</action>

<acceptance_criteria>
- Vite plugin function-length warnings are closed or materially reduced.
- `pnpm --filter @napplet/vite-plugin type-check` and workspace build pass.
</acceptance_criteria>
</task>

<task id="146-01-03">
<title>Document oversized-file deferrals</title>

<action>
Record exact remaining file-size warnings, why they are not low-risk to split in this milestone, and what a future structural milestone must preserve.
</action>

<acceptance_criteria>
- Remaining file-size warnings are reviewed and documented, not silent debt.
</acceptance_criteria>
</task>

<task id="146-01-04">
<title>Verify complexity cleanup</title>

<action>
Run the scanner, `pnpm -r type-check`, `pnpm -r build`, and `pnpm -r test:unit`.
</action>

<acceptance_criteria>
- Function-length warnings are closed or justified.
- Remaining file-size warnings have explicit deferrals.
- Workspace verification commands exit 0.
</acceptance_criteria>
</task>

## Verification

- `pnpm dlx aislop scan --json .`
- `pnpm -r type-check`
- `pnpm -r build`
- `pnpm -r test:unit`
- `git diff --check`

## Risks

- Public type/barrel file splits can change declaration emit or import contracts; defer when not clearly internal-only.
